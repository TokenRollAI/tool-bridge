# 当前状态(MUST)

> 用途:每次会话开场必读的易变状态快照(部署、代码现状、凭据配置、工具链、未竟事项)。更新时机:部署/凭据/工具链/能力面变化时,由当轮 Agent 更新本文件。最后增量核实:2026-08-11(SearchIndex v3/MCP discovery 共享开发部署与在线验收;其余历史状态以各条日期为准)。

## 项目状态

- 2026-08-11:**宿主中立层抽成独立包 `@tool-bridge/app`,gateway 降为薄 CF adapter**(架构评估建议 #1 的彻底版)——此前 tbApp/bootstrap/deviceHello/providers 这套宿主中立代码住在 gateway 里,sdk 与 server 跨过 exports 深链 `@tool-bridge/gateway/tbApp` 消费;包名/description/keywords 全说 cloudflare,gateway tsconfig 又全局挂 `types: ["@cloudflare/workers-types"]`,中立性只靠人工纪律。现搬 17 个文件(~6050 行)到 `packages/app`,其 tsconfig 刻意 `types: []` + `lib: ["ES2023","DOM"]`,**中立性改由类型系统保证**;gateway 只剩 ~1212 行真正的 CF 绑定(app/deviceSession/kvStateStore/r2Object/d1SearchIndex),exports 收回只剩 `.`。三个宿主包一律 devDependencies + `noExternal`(**core 是 private 包各自 bundle,app 若留 external 会并存两份 core,`err instanceof TBError` 跨副本恒 false,TBError 被静默降级成 internal;workspace 内跑源码单副本测不出**),三个 `tsconfig.build.json` 同步补 app 的 paths 映射(否则 rollup-dts 解析不到源码,d.ts 留悬空 import 而 app 不随发布走)。搬迁只暴露一个真 bug:`objectBodyToBytes` 返回 `Uint8Array<ArrayBufferLike>`,标准 `BodyInit` 拒收 shared-buffer 支撑视图(workers-types 恰好宽松放过),已在 core 修。产物对照 HEAD 实测基线零回归(gateway 452.43→452.66 KB,sdk 2.27 MB / server chunk 1.92 MB 持平,新增 app 403.67 KB);`pnpm verify` 13 任务全绿(**1291 passed / 7 skipped** + provision)。新增 `publish-app.yml`,三个发布 workflow 的 typecheck `--filter` 链改指 app。**测试这一步刻意不动**:app 的行为验证仍在 gateway 的 workerd 套件里,把验证面迁到中立宿主(评估建议 #2)是下一步。**app 0.1.0 待手动首发 + 配 Trusted Publisher;本轮未部署、未发布。**
- 2026-08-11:**内置插件重构为"单包源码文件夹"形态**(用户对照 open-connector 拍板)——废除"每插件一个 workspace 包"(package.json/tsconfig/vitest/wrangler/node_modules 全套壳),新建 `packages/plugins`(private):`src/<name>/` 每插件一个纯源码文件夹(现有 `src/notes/`、`src/feishu/`),`src/registry.ts` 是懒加载装配表(`BUILTIN_PLUGIN_LOADERS` + `builtinPluginBindings(env,{include})`,include 供 CF 按体积裁剪;规模大后改 codegen,对齐 open-connector 的 registry.generated 形态)。`plugins/*` workspace 目录已删除(workspace glob/Dockerfile COPY/根 filter 同步);feishu 独立 Worker 部署保留为 `wrangler.feishu.jsonc`(`pnpm --filter @tool-bridge/plugins deploy:feishu`);compose plugin 服务改跑 `packages/plugins/scripts/serve.ts`(@hono/node-server 起外挂 HTTP 形态,env 传参,已本地实跑探活 healthz/~describe);feishu 的 9 例 workerd 测试移植为纯 Node vitest(SELF.fetch→plugin.fetch shim,断言零改动全过);gateway 测试改 import `@tool-bridge/plugins/notes`。全量验证 13 任务全绿。**注意:compose 三跳栈尚未整栈重跑(仅 serve 进程级探活);生产 tb-plugin-feishu Worker 未重部署,部署入口已迁移。**
- 2026-08-11:**system/plugin `catalog` cmd + `tb plugin catalog` 已落地**(可用目录列举,CLI 同轮对等)——admin cmd 列出宿主装配的进程内插件(`bindings` 经 `BuiltinAssemblyOpts` 从 `pluginBindings` 接线),逐项对拍注册状态(registered/pluginId);binding 集成测试断言 available→registered 翻转,core/gateway 的 cmd 表钉住测试与 CLI strictParsing 叶子清单同步,协议契约文档已更新。全量验证 13 任务全绿。**后续**:构建期插件集合选择、plugins/ registry codegen 与脚手架、Dashboard Plugins 页 catalog 集成(可选)。
- 2026-08-11:**binding: 进程内插件传输已落地**(plugin-in-process-catalog 决策第一刀)——`TbAppDeps.pluginBindings`(binding 名→fetch handler,懒加载放 handler 闭包)注入后,`binding:<name>` manifest 的探活/`~describe` 契约抓取/envelope 调用全部进程内直调(此前 501),wire 契约与 HTTP 通道逐字节一致;未装配 binding fail closed(unavailable);外挂 https plugin 通道不变。集成测试把全局 fetch 打桩为抛错,证明注册→挂载→调用零网络出站(gateway 197 tests)。三宿主装配入口已铺通:`createApp(opts.pluginBindings)`(Workers)/ `createTbServer(config.pluginBindings)`(Node)/ `createToolBridge(config.pluginBindings)`(SDK),类型自 gateway 根与 tbApp 子导出。**后续**:builtin system/plugin 的"可用目录"列举(须 CLI 同轮对等)、构建期插件集合选择(CF 体积上限)、plugins/ 目录 registry codegen 与脚手架。
- 2026-08-11:**workspace 规模化基建第二步 + 手写代码收敛已落地**:①引入 **Turborepo**(`turbo.json`;所有包互相消费 TS 源码,故任务经 `dependsOn: ["^typecheck"]` 把上游文件哈希纳入缓存键,已用探针证实改 core 打掉全部下游 test 缓存而无关包命中;gateway 补 `@tool-bridge/dashboard` devDep 声明 ui.integration 的真实测试输入边),根 `typecheck`/`test` 走 turbo,热 verify 从分钟级降到秒级,pre-commit 全仓 typecheck 自动享受缓存;②`provision.mjs`:R2 存在性探测改 `r2 bucket info --json`(缺失 bucket 非零退出,已对真实 API 实测),`wrangler.jsonc` 回填改 **jsonc-parser** modify/applyEdits,fake-wrangler 回归已同步,真实幂等 provision 复跑全 skip 且零 diff;③core 三份 base64url 合一至 `encoding/base64url.ts`(统一严格语义:字符集+len%4≠1;旧宽松累加器会把非法长度静默解成截断字节再喂 AES-GCM/HMAC),secretStore 重导出保持公开面,新增往返/拒绝钉住测试;④CLI `parseIsoTimestamp` 委托 core `normalizeExpiresAt`(zod datetime 正则本身日历感知);⑤`matchGlob` 相邻 `**` 合并消除指数回溯 + >64 段 pattern fail closed(钉住测试);⑥短词 escaped-LIKE SQL 三件套抽到 core `search/shortTermSql.ts`,D1/SQLite 两 adapter 共享唯一真源。root devDeps 新增 turbo/jsonc-parser。全量验证:typecheck+lint+13 turbo test 任务+provision 全绿 exit 0(core 751)。**遗留**:skillhub frontmatter 换 `yaml` 需对"core 只依赖 zod"纪律做显式破例决策,未动。
- 2026-08-11:**workspace 规模化基建第一步已落地**(为"上千 plugin"做准备):①插件迁出 `packages/*` 到独立 `plugins/*` workspace 目录(`packages/plugins/src/notes`、`packages/plugins/src/feishu`;npm 包名不变),根 `test:integration` 改按目录 glob `--filter './plugins/*' --if-present` 选插件,Dockerfile 增 `COPY plugins`、compose plugin 服务路径同步;②共享依赖(≥2 包使用)版本收编进 `pnpm-workspace.yaml` catalog(gateway 的 zod 4 单列命名 catalog `zod4`,与 core 系 zod 3 的语义统一是未竟独立事项;dashboard `@types/node` 24→26 对齐),已实证 `pnpm pack` 会把 `catalog:` 改写为真实版本号(cli tarball 核对);③新增根 `tsconfig.plugin.json`(ES2023+DOM、无 Node/Workers 全局,与 plugin-sdk 宿主中立姿势一致),两插件 tsconfig 从两种漂移姿势统一为一行 extends;④新增 `.github/workflows/ci.yml`(main push + PR 跑 `pnpm verify`)并补根 `packageManager: pnpm@11.21.0`。每步后 `pnpm verify` 全绿(catalog 步实跑 exit 0:745+10+242+22+19+9+194+38 + provision)。既有 wrangler↔workers-types 的 peer 警告为存量,与本轮无关。后续步骤(插件脚手架、plugin-sdk Node serve 层与 multi-plugin host)见本轮评估,未实施。
- 2026-08-11:**共享开发环境已部署并完成 Phase 5 在线验收,但不是 production release**——用户明确项目仍在开发中并授权覆盖既有开发实例。真实 APAC D1 `tb-search` UUID=`f788b779-ec1c-4fba-ac1f-b780fab990fc`,二次 provision 精确 skip;plugin-feishu version=`140c2a97-3fbe-4faf-8e8a-7a7045606e67`,gateway version=`ce61da86-fb00-4550-915d-08eb4c7a7c94`。`pnpm smoke`、线上 MCP admin/narrow list+call(29→2)、D1 Search 两 query admin/narrow(2→1)、标准与 155s hibernation device、plugin/v2 飞书 create/fetch/update、窄 SK Dashboard exact-query DOM 均通过;临时 SK/registry/D1 fixture 已清理,飞书验收文档保留。线上 Dashboard console 会记录 Cloudflare 自动注入 Analytics beacon 被 `script-src 'self'` CSP 拦截,不得写成 console 0,也不应为消警放宽 CSP。[PR #35](https://github.com/TokenRollAI/tool-bridge/pull/35) 已合入 `main`;全局 Done 仅继续受外部 HTBP Draft 同步阻断。
- 2026-08-11:**SearchIndex v3 与 MCP 发现面三工具已部署到共享开发环境**——gateway version=`2221abb7-bd12-4c00-8bc2-8918d5bae9b4`;D1/SQLite 仅保存轻量 `path/name/description/feedback/toolDigest`,不再持久化完整 ToolSpec,网关在权限裁剪后从 canonical registry/tool cache 水合完整结果。无状态 `/~mcp` 新增稳定 `tb_search`、`tb_help`、`tb_list_nodes`,分别复用 `/~search`、`~help`、`~tree` 的现有认证与权限链。部署后 `pnpm smoke` 通过;真实 D1 v3 重建出 8 个飞书工具;API、真实 CLI 与 Dashboard DOM 均命中 `feishu:create-doc`,返回完整 8790-byte description;官方 MCP SDK `tools/list` 共 38 项,三个稳定工具均完成真实 call。线上 Dashboard 仍只有已知 Cloudflare Analytics CSP error。共享 adapter、D1/SQLite、官方 MCP SDK、长描述目录与窄 SK 回归均已覆盖;最新 `pnpm verify` 为 package **1276** + provision **1** = **1277 passed / 7 skipped**。
- 2026-08-11:**E2E-D 本地 final-image 一键栈证据已闭环**——`pnpm --filter @tool-bridge/server --prod deploy --legacy /out` 会留下指向 build workspace的 Dashboard symlink,该目标在 final stage不存在;production Dockerfile现将fresh-built `dist` 显式复制为 `/app/dashboard`,并设 `TB_UI_DIR=/app/dashboard`,由非root `node` 用户直接托管。final仍是单个 `node:22-bookworm-slim`进程、`CMD ["node","/app/dist/main.js"]`、单端口与仅 `/data` volume。Compose smoke除 gateway→plugin-feishu→mock upstream三跳外,还检查 `/ui/` 与SPA deep link `/ui/manage/registry` 的200 HTML/title。真实浏览器直接消费该 Compose final image,desktop/mobile共九路由无横向overflow;树请求边界为root=1/local=1/remote=3/filter=8,ARIA键盘与移动Escape焦点恢复通过,18次导航 `console warning=0/error=0`, `pageerror=0`。既有全量证据仍为 package **1273** + provision **1** = **1274 passed / 7 skipped**。**这只闭合本地E2E-D;浏览器流程尚未固化为仓库内Playwright spec,也不替代真实Cloudflare/D1/飞书/DO等其余生产PENDING。**
- 2026-08-11:**E2E-C 双宿主与三消费面已闭环**——shared contract、Miniflare D1、Node TCP/SQLite、真实 CLI 全 cursor 与本地 Dashboard 证据先行;Round 32 又在共享开发真实 D1 上以临时 visible/hidden fixture 对拍 `日程` / `create document`,admin 2→narrow 1,线上 Dashboard DOM 同样只见 allowed path。临时 SK、registry fixture 与 D1 行均已清理。
- 2026-08-11:**E2E-A 的确定性与在线证据已闭环**——gateway secretRef/device、Node bootstrap/DeviceHub barrier+mutation 与全包 38 tests 先行;Round 32 的共享开发 `smoke`、`verify-device` 标准路径和 `TB_VERIFY_HIBERNATION=1` 又验证真实 shell/fs、同 ID replacement、registerPaths allow/deny 及 155s 空闲后调用,临时设备与 SK 已清理。
- 2026-08-11:**localhost-only Docker Compose 三跳开发栈已本地落地并实跑闭环**——根 `docker-compose.yml` 启动 gateway production final image(仅 `127.0.0.1:8787` 发布)、内网真实 plugin-feishu Wrangler Worker与认证mock upstream;`smoke` profile通过 `docker compose run --rm` 同步验证Dashboard root/deep link、写SecretRef、注册plugin/v2、挂载export并调用出精确 `compose-roundtrip`,不是并列health假绿。`compose:up`启动三常驻服务,`compose:down`保留 `gateway-data`,`compose:reset`才删卷。证据覆盖fresh volume、同卷重复smoke、gateway restart、全新错误app id→upstream 401/gateway 503/进程非零后恢复、`HostIp=127.0.0.1`、final CMD与teardown。固定Admin/plugin/app/TAT fixture **只允许隔离localhost开发**,不得复制到生产。**mock route独立单测、smoke逐请求AbortSignal、并行worktree image tag与长期Playwright spec仍是增强项;本地证据不代表生产凭据、真实飞书、跨主机网络或部署。**
- 2026-08-11:**Dashboard Registry/Plugins/SK 系统表单拆分与管理面契约对拍已在本地完成**——wire payload 收敛到无 React/DOM 依赖的纯 builder;Registry 六 kind 字段与 `MountDialog`、Plugin manifest 字段与注册/编辑 dialog、SK 字段拆入 `pages/system/forms/`,三者共用 `FormSection`,route page 保留查询/mutation 编排及尚属本页的展示或 dialog。`NodePage` 与 Registry route 都直接依赖独立 `MountDialog`,不再从 route page re-export。对拍同时补齐 HTTP auth/ToolDef 校验、custom scheme 空前缀 fail closed、SK owner/每条 scope 半填 fail closed、registerPaths 按行解析以保留合法逗号,以及 plugin/v2 只抓 `~describe`、bearer 字段为 `secretRef`。Dashboard 已有 Node Vitest **10 passed**,并纳入根 `test:unit`;gateway fresh-source `ui.integration` **15/15**。真实浏览器核对 desktop/mobile 无横向溢出且 console 0。**Node 单测只证明 builder wire shape;dialog 生命周期、kind 草稿保留、Plugin 分页与一次性 token/secret 交互仍未被组件测试覆盖。**
- 2026-08-11:**root 全局工具搜索的自动同步、加权检索、权限后分页与 CLI/Dashboard/MCP 消费面已部署到共享开发环境**——StateStore/registry/tool cache 是 canonical,SearchIndex 是可重建派生状态;D1/SQLite v3 lightweight schema保留检索字段与完整 ToolSpec digest,长词 trigram FTS、短词 escaped LIKE、混合 AND。协议支持 limit/cursor(default 50/max 200),权限/virtualize 后分页与 AES-GCM cursor,并有 500 paths/20 KiB、raw work 400、完整响应 4 MiB、D1 Free 50 queries 等边界。当前仅 keyword;semantic 与外部 HTBP Draft 同步仍 PENDING。
- 2026-08-11:**工具搜索 D1 已真实 provision 并部署到共享开发环境**——`TB_SEARCH` → `tb-search` UUID=`f788b779-ec1c-4fba-ac1f-b780fab990fc`;首次 create 后配置回填,二次运行 KV/R2/D1 全部 skip。fake wrangler 幂等回归仍纳入 `test:unit`,真实 D1 则由 Round 32 双 query/admin-narrow smoke 验收。
- **初步实现阶段已完成**(2026-07-07 "破壳"):SK 鉴权与作用域、HTBP 核心树与内容协商、Tool 层(mcp/http/remote 联邦 + 虚拟化)、Context 层(r2/s3 四动词 + Search + `$ref` 大对象)、设备反向注册(DO WebSocket hibernation)、SDK、Plugin 系统、Dashboard 均已落地并经过在线开发环境验证。
- 2026-08-11:**安全报告部署 blocker 已闭环并在共享开发环境复验**——OAuth callback/CSP/安全头、Workers与Node bootstrap fail closed、Secret Reference 使用授权、canonical origin、RefToken用途域、DO/Node invoke scope/registerPaths与active-generation guard均已有测试;在线又完成 smoke、同 ID replacement 与155s hibernation。剩余风险是 KV最终一致、Dashboard localStorage SK、分布式 rate limit及未来正式 release 的 clean-main复验,不再是本轮部署 blocker。详见 [../memory/reflections/2026-07-24-security-report-remediation.md](../memory/reflections/2026-07-24-security-report-remediation.md)。
- 2026-07-22:**CLI 参数契约审查与修复已在本地完成,0.8.0 发布物已准备**——①SK `expiresAt` 在 CLI 与 core Write/Update 双层只接受带时区 ISO timestamp并规范为 UTC,认证读取对历史非法值 fail closed;②`--json/--base-url/--sk/--timeout` 可放根/命令组/叶子位置,Commander 解析错误在 `--json` 下也结构化且只认解析后的 option value/source(`--` 后同名文本不误触发),组级 help 展示 Global Options;`server add` 远端地址改为 `--remote-url`(`--base-url` 恒指当前网关,help 与缺参错误均含迁移提示);③status/login/tool auth 一次性 HTTP 接入统一 timeout,长驻 connect 明确拒绝不适用的 `--timeout`;④mcp/http/tool、r2/s3/plugin 等条件 flag 与多输入源本地拒绝冲突,挂载省略描述时派生非空值,tree 显式 depth 严格 1..8,help 同步依赖/互斥/范围/默认值与分页 fallback;⑤返回 Page 的 SK/Secret/Plugin/Context/Skill/Server/Device list/search 统一 `--limit 1..200`/`--cursor` 且过滤列表不丢 cursor;⑥补齐 `ctx rm`、SK get/update/enable/disable、tool/context Plugin Provider 挂载。`pnpm verify` 全绿(见常用命令),CLI 21 文件 **233 passed**;`packages/cli/package.json` manifest 已 bump 到 **0.8.0**,CLI build、实际入口 `--version`(回报 0.8.0)与 `npm pack --dry-run --json` 均通过。**尚未打 `cli-v0.8.0` tag、尚未发布/部署**;npm registry `latest` 仍是 **0.7.0**,发布时须明确 `server add --base-url` → `--remote-url` 迁移。
- 2026-07-07:修复"挂载 MCP 过一段时间失效"生产故障(不合规上游对过期会话回 200+空列表,见 [../guides/mcp-upstream-pitfalls.md](../guides/mcp-upstream-pitfalls.md))。2026-07-08 同故障复发:初版防御的重试回读 KV 被边缘读缓存击穿(拿回刚删的旧会话),已改为重试强制完整重握手(`forceFresh`,PR #8)并补钉死用例,**已部署上线**且经塞伪 session 复现验证自愈(工具列表恢复、KV 回填新会话)。
- 2026-07-08:`~help` 可读性重构:新增 `Accept: text/markdown` 可读表现(renderHelpMarkdown)、`hint` 行/字段(下一步指引)、索引形态 h 一句话摘要(此前上游 mcp 整篇多行 description 原样撑爆索引并破坏 DSL 行结构)、DSL 多行值安全化(node 行折叠单行、多行 h 续行缩进)、`tb help --md`;契约见 [../reference/protocol-contract.md](../reference/protocol-contract.md)。**已部署上线**(smoke 通过;生产实测根/mcp 索引/单工具三级 markdown 与索引一句话化均生效)。
- **Docker/Node 宿主部署路径已落地**(2026-07-08):`@tool-bridge/server` 包(SQLite + FS + ws DeviceHub)+ 根 Dockerfile + GHCR/npm 双发布 workflow;本机与 Docker 验收全过(smoke/verify-device/verify-plugin/重启持久)。见 [../guides/docker-host.md](../guides/docker-host.md)。
- 2026-07-08:**Path 补充说明 + Agent 反馈能力**落地(本轮)——①builtin `system/annotation`(七模块):admin 对任意 path(含工具子路径、根=全树公告)写补充说明,网关 `enrichHelp` 注入该 path `~help`(DSL `note` 行 / JSON `note` / Markdown Notes 节);②**`~feedback` 新保留段**(per-path 一级协议能力,非 builtin):`GET /<path>/~feedback`(列表,?hidden=1)/`GET .../<id>`(详情)/`POST`(提交 {title≤80,detail≤500},call scope)/`POST .../<id>`(投票 up|down|clear,每 owner 一票可改票)/`DELETE .../<id>`(admin);净分>-3 前 5 条注入 `~help` feedback 块(带 use 指引行,未知行忽略通道,DSL↔JSON 等价);权限判定落目标 path(窄 scope SK 天然可用);存储 `annotation:<path>` 单条 + `feedback:<path>` 单 key 数组(KV last-write-wins 接受并发窗口);CLI `tb note`/`tb feedback` + Dashboard NodePage(NoteCard + 反馈 tab)三入口对等;根 `~help` hint 与 `tb --help` 尾部文案双向引导 feedback 习惯(用前查经验、踩坑后回馈)。契约见 [../reference/protocol-contract.md](../reference/protocol-contract.md)。**已部署上线**(首次随 Worker version 43 生效;2026-07-10 生产只读复验 `GET /system/annotation/~help` 与节点 `GET /~feedback` 均为 200;此前本机 Node 宿主全链路 E2E 与 Dashboard 实操均已通过)。
- 2026-07-16:**skillhub 新 kind 落地**(本轮,与 context/device 并列的一等内容型 kind:Agent Skill 仓库)——每个 skill = 一个目录 `<id>/SKILL.md`(Claude 约定的 frontmatter name/description)+ 若干 UTF-8 文本文件;发布到平台自带 R2(provider r2,**无需外部凭证**;s3 可 opt-in)。存储引擎完全复用 context 的 ObjectStore/ObjectContextProvider(etag 版本、`$ref`/`~ref` 大对象、按 `skills/<path>` 前缀隔离、ttl/readOnly);唯一净新逻辑是 core `skillhub/`(frontmatter 最小解析器 + `createSkillhubProvider` 的"以 skill 为单位"分组)。数据面动词:`List`/`Get`(含 `{id,file}` 取单文件)/`Search`(read)+ `Publish`(整体替换,校验含 SKILL.md 且 name/description 必填)/`Remove`(write);服务端解析 frontmatter → `List` 目录(id/name/description/version)、`Search` 覆盖 name/description(渐进式发现)。三入口对等:CLI `tb skill mount/publish/ls/get --out/search/rm/unmount`(`get --out` 把 skill 落到本地 `.claude/skills/<id>/` 直接可用)+ Dashboard(KindBadge/NodePage SkillBrowser/RegistryPage 挂载表单全集成)+ API。核心接线与既有 `tool` kind 同路径(`types.ts` NodeKind/NODE_KINDS/NodeConfig + 网关 handleInvoke/helpModelFor/handleDescribe/assertSkillhubConfig/`~ref` 泛化);`~skill` 保留段(节点使用指南)与 `skillhub` kind 语义正交,不冲突。新增 gateway `skillhub.integration.test.ts`(9 例,真实 workerd + 本地 R2)+ cli `skillhub.test.ts`(12 例);`~skill` 保留段本地仍 501 占位未变。代码已包含在 Round 32 gateway 部署中,但本轮未创建或操作共享开发环境的 skillhub 挂载。
- bootstrap 期过程文档已归档 `archive/`;知识真源 = 代码 + llmdoc(见 [project-brief.md](project-brief.md))。
- 2026-07-08:**CLI 与 API 描述面向 Agent 重整**(本轮)——①builtin 七模块(sk/secret/registry/status/plugin/federation/annotation)每 cmd 补 `h` 一句话说明,inputSchema 属性全量补 `description`(owner ref 格式、Scope 结构 items schema、opts 分页语义等;新增 `util.ts` `LIST_OPTS_SCHEMA` 共用分页 schema,sk/registry 的 write/update.patch 共享字段 schema 常量);②context 六动词 `h` 与 device shell(`describeAllow`/exec/`SHELL_DESCRIPTION`)文案由中文改英文(~help 消费方是任意 Agent,统一英文口径;shellExecutor permission_denied 消息同步,4 处钉住测试已同步);③CLI:顶层 description 加起步指引(login→ls/help→call),call/sk create/tool mount/ctx/connect 补 `addHelpText` Examples,whoami 描述去噪。**已发布/部署**(CLI 由 npm 0.6.1 承载,API 描述首次随 Worker version 43 生效)。
- 2026-07-08:**CLI markdown 终端富渲染**(本轮)——`tb help` 与 `tb call`(人类模式)的 markdown 输出按 stdout 形态分流:TTY → marked-terminal 渲染 ANSI 富文本(标题着色、表格盒线、代码高亮),管道/重定向/Agent 捕获或 `NO_COLOR` → 裸 markdown 原样(对机器最优形态);`tb help --md` 语义从"无操作别名"变为**强制裸 markdown**(TTY 下也不渲染,便于复制/落文件)。落点 `cli/src/markdown.ts`(`printMarkdown`/`shouldRenderAnsi`,渲染失败降级裸输出);新依赖 marked@15(marked-terminal peer 上限 <16,勿升 16+)+ marked-terminal@7(@types/marked-terminal 的返回类型标错需断言桥接)。**已发布**(`@tool-bridge/cli` 0.6.1;verify 全绿,TTY/管道双形态已实测)。
- 2026-07-08:**直连工具调用**上线(PR #9)——`POST /<node>/<tool>` body 即 arguments 本体,`~help` 宣告直连路径(CmdSpec `flatBody`);信封入口 `POST /<node>` + `{tool,arguments}` 保留。CLI(`tb call <node>/<tool>`,--tool 变 optional)与 Dashboard(CmdPanel 按 cmd.path 判别、CliHint 同步)同轮对齐。已部署上线并经生产直连调用实测。
- 2026-07-08:**mcp 托管 OAuth 上线并经真实上游验证**——`config.auth:'oauth'` 的 mcp 挂载由网关全托管授权码流程(SDK auth() 编排 discovery+DCR+PKCE;`POST /<path>/~authorize` 发起、`GET /~oauth/callback` 回调,state 为 AES-GCM 加密自包含载荷零 KV 存储;token/client/discovery 落 `mcpoauth:*`,SDK 自动刷新;CLI `tb tool mount --auth oauth` + `tb tool auth`;Dashboard 挂载面认证三选 + 列表钥匙按钮)。**严格上游降级通道**:Bytebase 等上游 DCR 硬编码 redirect 白名单只放行 loopback/知名客户端(不可配置),`~authorize` 可带 body `{redirectUri}`(仅 loopback)+ `tb tool auth --local`(本机临时端口收回跳,code 转交网关兑换)。已部署并经生产 Bytebase 实测全链路(--local 授权 → 工具列表 → 真实调用)。已知限制:refresh token rotation 上游 + 多 isolate 并发刷新可能互相作废(失败回「重新授权」指引可自救)。
- 2026-07-08:**内容协商默认翻转为 markdown**——所有端点默认(无 Accept/`*/*`/未知类型)返回 `text/markdown`,声明 `Accept: application/json` 才回 JSON;紧凑 Help DSL 保留但降为显式通道(`Accept: text/plain`;`~tree` 同理,markdown 表现为 code fence 包缩进树)。归类唯一入口 core `htbp/negotiate.ts`。CLI:`tb help` 默认 markdown、新增 `--dsl`、`--md` 变默认别名;Dashboard:节点页「~help 原文」DSL tab 换成「~help 文档」markdown tab(react-markdown 渲染)。内部消费不受影响(plugin 契约抓取与 Dashboard 表单显式 Accept json;remote 透传原样转发 Accept)。契约见 [../reference/protocol-contract.md](../reference/protocol-contract.md)。**已部署上线**(2026-07-08:smoke 通过含新双表现断言;生产 curl 实测默认 markdown / text/plain DSL / json / ~tree fence 四形态均生效)。
- 2026-07-08:remote 联邦 host 白名单从"部署期 env-only"扩为"env 基线 ∪ 运行时可增删"——新增 builtin `system/federation`(admin scope)+ `RemoteAllowlistStore`,三入口对等(`tb federation ls|add|rm` + Dashboard「联邦白名单」页 + API);env 基线条目只读不可删。**已部署上线**(evilstar 账户)。
- 2026-07-08:Dashboard 树发现性能修复(远端联邦树慢)——根因是根 `~tree` 聚合 remote 子树时 `getChildren` 对**每个**远端节点单独 fetch 上游(N+1,首屏 21s;上游自身仅 1.8s)。修复两处:①前端 TreeNav 改**懒加载**(首屏 depth=1 不碰远端,展开某节点按需拉;remote 节点/其后代走纯透传 depth=3,本地目录 depth=1 免 N+1;NodePage/TreeNav 对纯透传返回的远端 path 做 localize 补挂载前缀);②core `buildTree` 加 `opaqueKinds`——深度边界(`depthLeft<=0`)对 remote 节点免 fetch 直接标 truncated(gateway 传 `{'remote'}`),消除边界探测的远端往返。实测首屏 21s→<1s(热)、展开 remote 目录 7s→<1s。**已部署上线**。
- **npm 已发布**(四包均经 CI Trusted Publishing;core 为 private 不发布):`@tool-bridge/cli` 0.7.0(2026-07-10,Agent 体验修复:positional args / retryable / --timeout / feedback hint;tag `cli-v0.7.0`,run `29092102298` 成功且 npm `latest=0.7.0`)、`@tool-bridge/sdk` 0.4.0、`@tool-bridge/gateway` 0.4.0、`@tool-bridge/dashboard` 0.6.0(2026-07-10,tree-first 整站大修;tag `dashboard-v0.6.0`,Trusted Publishing run `29068385868` 成功且 npm `latest=0.6.0`)。gateway/dashboard 的 Trusted Publisher 已配置生效;`@tool-bridge/server` 0.1.0 与 `@tool-bridge/app` 0.1.0 可发布,**均待手动首发 + 配 Trusted Publisher**。发布流程见 [../guides/npm-publish.md](../guides/npm-publish.md)。**坑:一次推多个 tag(`git push origin tag1 tag2 …`)不触发 tag workflows,须逐个 push**(2026-07-08 实测:四 tag 同推零触发,删除后逐个重推全部触发;同日逐个推四 tag 复验全部正常触发)。
- 2026-07-08:**mcp 上游自定义请求头**落地——mcp NodeConfig 新增 `authHeader`/`authScheme`(与 http 同语义)与静态明文 `headers`(凭证头覆盖同名),动机是接入飞书官方 MCP(`X-Lark-MCP-UAT/TAT` 原样注入 + 必带 `X-Lark-MCP-Allowed-Tools`,见 [../guides/mcp-upstream-pitfalls.md](../guides/mcp-upstream-pitfalls.md));CLI(`--auth-header/--auth-scheme` 变 mcp/http 共用、`--header` 仅 mcp)与 Dashboard 挂载表单同轮对等。**已部署上线**(2026-07-08 随 0.3.0 内容协商部署一并上线);飞书未走直挂形态(生产采用 plugin-feishu,见下条),直挂静态凭证通道保留未实际使用。该扩展保留不回滚:直挂静态凭证的 MCP 上游仍是合法路径。
- 2026-07-08:**plugin-feishu 新包**(commit ae8ab02)——飞书官方远程 MCP 的 tool-provider/v1 plugin(CF Worker,自部署进用户 CF 账户),动机:TAT 约 2h 过期,plugin 内自动换发 + 上游 401 强制重换发自愈,免人工 `tb secret set` 续期;凭证会过期的上游推荐此形态,直挂 kind:mcp + 静态 TAT 适合一次性验证。**已部署已注册已挂载**(2026-07-08 实测)——Worker `tb-plugin-feishu` @ https://tb-plugin-feishu.shuaiqijianhao.workers.dev(DJJ 账户);生产网关 `tb plugin register`(id=feishu,platform-token)+ 挂载树节点 `feishu`(kind:tool,authRef=feishu-app);`tb help feishu` 经完整链路(网关→plugin→TAT 换发→飞书 MCP)列出 8 工具;**业务调用已生产复验通过**(2026-07-08,用户开通 docx 应用权限后):create-doc 建档、fetch-doc 读回、update-doc append 追加(注意 `mode` 参数必填)全链路成功;list-docs 仍差 drive:drive 权限(99991672,用户按需开通)。**注意**:TAT 应用身份创建的文档归属应用,用户账号默认不可见,须 create-doc 传 `folder_token`/`wiki_node` 落到用户可见目录(应用需先被加为该目录协作者)或经 add-comments @用户 分享。
- 2026-07-08:**plugin 上游凭证边界重构**(commit 8af6914,已生产迁移 + 终验)——plugin 上游凭证(飞书 app_id/app_secret)不再由 plugin Worker secret 自持,改存平台 SecretStore,kind:'tool' config 新增 `authRef`,网关每次调用 resolve 后经新头 `X-TB-Upstream-Auth`(base64url)注入 plugin(resolve 失败 → unavailable 快速失败);动机:公网 endpoint 的 plugin 自持凭证时 PLUGIN_TOKEN 泄漏即凭证可用、换凭证须重部署,现在 plugin 无凭证即空壳,轮换只需 `tb secret set`。plugin-feishu 同步:TAT 缓存与 MCP 会话按 app_id 键控(多凭证挂载不串号,有测试钉住)。生产迁移完成:网关与 plugin 重部署、plugin 三 secret 删至仅剩 PLUGIN_TOKEN、挂载节点 `feishu` 配 `authRef:"feishu-app"`、`tb secret set feishu-app`(JSON {app_id,app_secret})后 create-doc 经新路径写团队 wiki 终验通过。
- 2026-07-10:**Dashboard 0.6.0 整站大修已发布并在生产生效**——信息架构重整为 `ActivityRail` + 可折叠 `ExplorerPanel` + 全宽 Workspace,移动端资源抽屉关闭后恢复焦点;TreeNav 保持 root depth=1、本地截断懒取 depth=1、remote 及后代纯透传 depth=3、仅非空过滤取 root depth=8,并以受控展开/可见顺序/焦点状态实现 ARIA tree roving focus。`NodePage` 继续作为通用协议回退,`CommandWorkspace` 只挂当前 `CmdPanel`,Context 使用桌面 master-detail/移动 dialog 且保持正文/version/`$ref` 原子基线;六个系统页对齐 builtin/CLI 的已知工作流、cursor 边界与危险操作 Promise 结算,历史/session/lazy route 安全边界延续 0.5.0。真实浏览器终验覆盖桌面/移动全部路由、树请求边界与键盘导航,无 document 横向溢出且 console 无 error/warning;SK 一次性值、Secret upsert 与 Registry replace 警告均已实操。`pnpm verify` 全仓 **1005 passed / 7 skipped**,退出码 0。发布证据:commit `e363e01`,tag `dashboard-v0.6.0`,Actions run `29068385868` 成功,npm `@tool-bridge/dashboard` 的 `latest=0.6.0`;生产 `/ui/` 首次由 Worker version 44(`eb4e8daf-7b5d-44ff-a91b-5b8074348854`)承载,HTML SHA-256 为 `2420378ce98842928fdad91c5ad87ae2f3956bb83c2853c929f1abd7d550a264`,生产 smoke 通过。后续纯文档提交可能生成内容等价的新 Worker version,故当前部署序号应以 `wrangler deployments list` 为准;Gateway `/healthz` 版本仍为 0.4.0。
- 2026-07-10:**CLI Agent 体验修复**(本轮,源自真实 agent 使用反馈:参数试错/超时不可控/retryable 丢失/feedback 机制零使用)——①`tb call` 第二 positional 收裸 JSON arguments(`tb call <path> '{...}'`;与 `--args`/`--args-file` 三源互斥,误写 `--json '{...}'` 形态自然工作,严格解析不破坏:第三个 positional 仍硬失败);②CLI 错误呈现 TBError `retryable`(此前在 `toCliError` 被丢弃):人类模式加 `(retryable — try again)` 尾注、`--json` 错误对象带 `retryable` 字段;③全局 `--timeout <seconds>`(默认 120s,`AbortSignal.timeout` 实现,超时报 retryable 错误并提示加大 --timeout);④**失败现场 feedback 引导**:`tb call` 失败(unavailable/internal/invalid_argument/rate_limited)时尽力拉取该 path `~feedback`(限时 5s、失败静默),有条目 stderr 列 top 3 + `tb feedback get` 下钻命令(--json 带结构化 `feedback`),无条目引导 `tb feedback submit`——把已建成但闲置的经验闭环推到 agent 注意力所在的报错现场;⑤core 上游错误 message 英文化(upstream unavailable/returned HTTP N,统一英文口径)。落点:cli `http.ts`(CliError+retryable/hint/feedback、apiFetch 超时)/ `args.ts`(--timeout)/ `output.ts`(reportError)/ `commands/call.ts`(positional+attachFeedbackHint)、core `tool/upstreamError.ts`。新增 `test/callUx.test.ts`(16 例),CLI 178 测试全绿,verify 全仓通过;生产实测 positional 双形态/超时/invalid_argument 触发 submit 引导。**已发布**(`@tool-bridge/cli` 0.7.0,PR #22 merge 后 tag `cli-v0.7.0` 触发 run `29092102298` 成功,npm `latest=0.7.0`)。

## 已部署资源(DJJ 账户)

| 资源 | 名称/地址 | 备注 |
|---|---|---|
| Worker | `tb-gateway` @ https://tool-bridge.pdjjq.org | custom domain(zone pdjjq.org);`wrangler.jsonc` 已写死 `account_id`;DO `DeviceSession` 绑定 `TB_DEVICE`(migration v1,sqlite);Dashboard 经 Static Assets 挂 `/ui`(`run_worker_first: true`);Dashboard 0.6.0 的上线证据为 Worker version 44(`eb4e8daf-7b5d-44ff-a91b-5b8074348854`),后续内容等价部署不改变该发布事实;运行时 `/healthz` 版本属 Gateway 0.4.0,Static Assets 中 Dashboard 为 0.6.0(两者版本归属不可混用) |
| Worker secrets | `TB_BOOTSTRAP_ADMIN_SK` / `TB_SECRET_ENCRYPTION_KEY` | 已 `wrangler secret put`;前者是 Admin SK 明文(引导时 sha256 入库) |
| KV | `tb-kv`(id `d18c93de33cf4ba2b1fbf7d26fd742f1`) | 绑定名 `TB_KV`;id 已回填 wrangler.jsonc |
| R2 | `tb-r2` | 绑定名 `TB_R2` |
| Worker | `tb-plugin-feishu` @ https://tb-plugin-feishu.shuaiqijianhao.workers.dev | 飞书 MCP tool-provider plugin,secrets 仅 `PLUGIN_TOKEN`(飞书凭证存平台 SecretStore `feishu-app`,经挂载 authRef 注入),生产网关注册 id=feishu、挂载路径 `feishu`(authRef=feishu-app) |

## 代码现状(pnpm monorepo:核心包与内置插件均在 `packages/*`(插件为 `packages/plugins/src/<name>/` 源码文件夹);共享依赖版本唯一真源是 `pnpm-workspace.yaml` 的 catalog,包内写 `catalog:`;插件 tsconfig 统一 extends 根 `tsconfig.plugin.json`。测试数为 2026-08-11 实跑)

- `packages/core` — 纯逻辑内核(唯一运行时依赖 zod),**745 个单测**,模块族:
  - `auth/`(scope 判定 / authorizer / 注册路径规则 / sk 签发与哈希)、`tree/`(path 规则 / NodeRegistryStore / visibility 裁剪)、`htbp/`(helpDsl / helpMarkdown / summary / HelpModel / negotiate / tree 构建)、`secret/`(AES-256-GCM 只写不读)
  - `builtin/`(**七模块**:sk / secret / registry / status / plugin / federation / annotation 的 cmd 表 + dispatch)、`annotation/`(AnnotationStore)、`feedback/`(FeedbackStore:排序/阈值/top-5 选条真源)
  - `tool/`(HttpToolDef 拼装、虚拟化、mcp schema→HelpModel、remote 路径/白名单/Via、上游错误归一、**RemoteAllowlistStore** 运行时白名单存储);`search/`(lightweight candidate `SearchIndex` + snapshot `MutableSearchIndex`,FTS/LIKE hybrid、snapshot/digest/budget、AES-GCM cursor 与分页 contract;完整 ToolSpec 由宿主从 canonical state 水合)
  - `context/`(四动词 objectProvider / objectStore 接口 / path 穿越防护 / ttl)
  - `device/`(帧编解码 / 会话状态机 / 设备侧 client / shell 白名单 / helpModel)
  - `plugin/`(manifest 校验 / envelope 编解码 / RequestDedupe / 契约校验)
  - `node/`(`./node` 子导出:FsObjectStore realpath 防逃逸 + shellExecutor 有界缓冲)
  - 顶层 `errors.ts`(TBError)/ `store.ts`(StateStore 接口 + 内存实现)/ `types.ts`。
- `packages/app` — **宿主中立应用层**(可发布,0.1.0 未首发;tsup `platform: 'neutral'` bundle core;tsconfig `types: []` 让编译器而非纪律保证中立):`tbApp.ts`(~2900 行,`createTbApp` 承载全部协议行为,含 root `~search` 的 bounded audit、权限后 over-fetch/canonical hydration/cursor)、`bootstrap.ts`、`deviceHello.ts`、`mcpServer.ts`(向无状态 `/~mcp` 投影稳定 `tb_search`/`tb_help`/`tb_list_nodes`)、`oauth.ts`、`refToken.ts`、`search/synchronizer.ts`(registry/tool cache/device/feedback 自动投影、批量 canonical ToolSpec 读取与 LKG 恢复)、`providers/`(mcp/http/remote/toolCache/s3Object/s3Sign/pluginClient/pluginTool/pluginContext)。**无自有测试目录**,行为由 gateway 的 workerd 套件覆盖。
- `packages/gateway` — Workers 宿主适配器(可发布 Worker library:tsup 单文件 ESM bundle core+app,`src/index.ts` export `createApp`/`type Env`/DO;exports 仅 `.`,发布形态 publishConfig 覆盖指 dist),约 1200 行:`app.ts` 做 Env→deps 适配并在可选 `TB_SEARCH: D1Database` 存在时注入 `D1SearchIndex`;另有 `deviceSession.ts`(DO/WS hibernation)、`kvStateStore.ts`、`providers/r2Object.ts`、`search/d1SearchIndex.ts`(v3 lightweight source/FTS/meta/snapshot adapter)。**其 197 例 workerd 集成测试同时是 app 中立层的事实验证面**:shared/SELF/HTTP、官方 MCP SDK、并发 capacity、自动同步、长描述目录、反馈权重、分页与预算测试已覆盖。`wrangler.jsonc` 已绑定真实 `tb-search` D1 UUID `f788b779-ec1c-4fba-ac1f-b780fab990fc`;v3 已随 gateway version `2221abb7-bd12-4c00-8bc2-8918d5bae9b4` 部署并完成在线对拍。
- `packages/cli` — commander 框架(manifest **0.8.0 prepared,未发布**;registry `latest=0.7.0`;严格解析:未知 flag/子命令报错,防权限误配),**242 个单测(22 files)**(含动态叶子命令 strictParsing、root search wire/输出、callUx、argSemantics 根/组/叶参数化与 `--` 边界、helpContract 的依赖/互斥/范围/默认值/迁移/fallback 回归),**22 个顶层命令族**:status / login / whoami / use / sk / secret / federation / note / feedback / ls / tree / search / help / call / tool / server / ctx / skill / connect / device / mount / plugin;全局 `--json` / `--base-url` / `--sk` / `--timeout`;配置 `~/.config/tool-bridge/config.json`(XDG,多 profile)。
- `packages/sdk` — 薄装配层(4 个源文件),**19 个通过 + 1 个 opt-in skipped**;公开面 `createToolBridge(config)` → `{ fetch, registerTool, registerContext, connect }`,复用 core + app 的 createTbApp;再导出内存宿主实现(MemoryStateStore 等)。
- `packages/dashboard` — React 19 + Vite + Tailwind 4 + shadcn/ui + @rjsf + TanStack Query SPA(可发布纯静态产物包:只发 dist,依赖全在 devDependencies),另有 Node 环境 Vitest **10 tests**覆盖 Registry/Plugin/SK 纯 builder并已纳入根 `test:unit`:tree-first 工作台由 `AppShell` 组合 `ActivityRail`、可折叠 `ExplorerPanel` 与 Workspace,移动端资源抽屉保持关闭焦点恢复;`TreeNav` 按 root=1/local=1/remote=3/nonempty-filter=8 的查询边界懒取并 localize remote path,受控状态统一 expanded、可见顺序与 ARIA tree roving focus。独立 `/search` 当前消费 keyword root search,区分未提交/loading/401·403/404/空态并按 opaque cursor 续页;点击结果导航 NodePage并以 `?tool` 预选命令,TreePath 在 UI 导航与 HTBP API 请求前都逐段编码。`NodePage` 是未知节点的通用协议回退并直接复用独立 `MountDialog`,`CommandWorkspace` 只挂当前 `CmdPanel`,`CmdPanel` 的 mutation→invalidate 由独立 `mutateAsync` Promise 链结算;工具名含 `/` 时不冒充单个 URL segment,自动退回节点信封调用与 JSON 参数编辑。Context 浏览器为 master-detail/dialog,List/Search/cursor/metadata/`$ref` 与正文/version/`$ref` 原子基线同在。SK·Registry·Devices·Secrets·Plugins·Federation 六页对齐 builtin/CLI 的已知工作流、cursor 知识边界和危险/并发操作语义;其中 Registry/Plugins/SK 已围绕 route coordinator + pure builder + 抽出的 kind/field/dialog 子组件重组,Devices 不虚构递归删除,Federation env 只读。调用历史 v2、profile/BaseURL/revision 隔离、路由 lazy 与 `AppErrorBoundary` 安全边界继续保留。无专用后端(同源消费 HTBP);gateway Vitest 每次从当前 Dashboard source 重建后再跑 `ui.integration.test.ts`,真实浏览器已覆盖 desktop/mobile、搜索分页与结果预选,且 console 0、无横向溢出。`pnpm deploy:all` 先 build dashboard 再部署 gateway。
- `packages/plugins/src/feishu` — 飞书 tool-provider plugin(private,CF Worker),**9 个集成测试**(真实 workerd,mock 换发接口与 MCP 上游,默认离线):TAT 自动换发与缓存、契约面/envelope、401 强制重换发自愈、凭证头缺失/坏形状、多租户按 app_id 键控不串号;文件职责见 code-map。
- `packages/server` — Node/Docker 宿主胶水(可发布 bin `tool-bridge-server`,0.1.0):`sqliteStateStore.ts` 与 v3 `sqliteSearchIndex.ts` 共用 `state.sqlite3` 文件但各持独立连接/独立表,后者实现 lightweight ToolSpec projection/feedback FTS/meta/digest snapshot、事务化容量与 cursor,由 `server.ts` 注入/关闭;旧 full-row 搜索表不迁移,首次 canonical audit重建。共享 adapter contract、同库异表、自动 registry sync、真实 HTTP root search 与 restart persistence 均有测试。首次引导缺 `TB_BOOTSTRAP_ADMIN_SK` 默认 fail closed且不监听/不打印明文;仅显式 `TB_ALLOW_INSECURE_BOOTSTRAP=true` 保留随机生成并打印一次的本地逃生阀。`DeviceHub.invoke` 在异步认证后复核 scope/registerPaths 与 active connection generation,barrier + mutation probe钉住同 ID 替换窗口;server 全包当前 **38 passed**。其余宿主面仍为 `config.ts` / `objects.ts` / `deviceHub.ts` / `assets.ts` / `server.ts` + `main.ts`;tsup bundle core+app(均 noExternal),better-sqlite3/ws/hono 等留 external;根 Dockerfile 产出单容器镜像。宿主中立 `runBootstrap` 与未传 `adminSk` 的 SDK 仍保留随机兼容行为,不得与 Node server 默认值混同。详见 [../guides/docker-host.md](../guides/docker-host.md)。
- `scripts/` — `gen-dev-vars.mjs` / `provision.mjs`(KV/R2/D1 幂等创建与 KV/D1 id 回填;`provision.test.mjs` fake wrangler 二次运行回归)/ `smoke.ts`(要求显式 base URL + SK;healthz + 无 SK 401 + 带 SK markdown/DSL 200,缺任一参数网络前 fail closed)+ 可重跑验收脚本:`verify-revocation.ts` / `verify-device.ts` / `verify-plugin.ts`(通用 context plugin/v2,不是飞书三动词)/ `verify-mcp.ts`(官方SDK双身份list+call)/ `verify-search.ts`(只读 exact-query admin/narrow CLI全分页对拍;不创建fixture)。
- CI(.github/workflows/):`ci.yml`(main push + 全部 PR 跑 `pnpm verify`,2026-08-11 新增)+ `publish-cli.yml`(tag `cli-v*`)/ `publish-sdk.yml`(tag `sdk-v*`)/ `publish-app.yml`(tag `app-v*`,2026-08-11 新增)/ `publish-gateway.yml`(tag `gateway-v*`)/ `publish-dashboard.yml`(tag `dashboard-v*`)/ `publish-server.yml` + `publish-docker.yml`(同 tag `server-v*`:npm 发布 + GHCR 镜像 `ghcr.io/tokenrollai/tool-bridge`),npm Trusted Publishing(OIDC)。
- 工具链:lint 用 ESLint 9;测试 vitest 4 + @cloudflare/vitest-pool-workers 0.18(API 变更注意见 [../guides/workers-kv-pitfalls.md](../guides/workers-kv-pitfalls.md))。

## 常用命令

- `pnpm verify` — typecheck + lint + 单测 + 集成测试一把过(2026-08-11 SearchIndex v3/MCP discovery 分支复跑:包测试 **1276 passed** + provision **1 passed** = **1277 passed / 7 skipped**,退出码 0)。
- `pnpm test:provision` — fake wrangler 验证 KV/R2/D1 首次创建、id 回填与二次运行不重建;已纳入 `test:unit`。
- `pnpm deploy:all` — 幂等 provision(KV/R2/D1) + dashboard build + 部署 gateway;会操作真实 Cloudflare 资源。
- `pnpm --filter @tool-bridge/server start` — 本机起 Node 宿主(默认 :8787,数据落 ./data;env 面见 [../guides/docker-host.md](../guides/docker-host.md))。
- `docker build -t tool-bridge . && docker run -p 8787:8787 -v tbdata:/data …` — Docker 路径,验收命令全套见 [../guides/docker-host.md](../guides/docker-host.md)。
- `pnpm compose:up` / `compose:smoke` / `compose:down` / `compose:reset` — 本地三跳开发栈;分别启动常驻服务、同步 one-shot smoke、保留卷停栈、删卷重置。仅 gateway 绑定 localhost,详见 Docker guide。
- `TB_BASE_URL=https://tool-bridge.pdjjq.org TB_SK='…' pnpm smoke` — 线上冒烟(**smoke 不读 .env,两者都须显式传;缺任一项网络前 exit 1,不跳过认证检查**)。
- `npx tsx scripts/verify-revocation.ts` / `verify-device.ts` / `verify-plugin.ts` — 可重跑在线环境验收(需 TB_BASE_URL + TB_SK,会消耗目标环境真实资源)。
- `TB_BASE_URL=… TB_SK=… TB_MCP_NARROW_SK=… pnpm verify:mcp` — 在线 MCP consumer 出口验收;窄 SK 默认须能读 `system/status:get`,或用 `TB_MCP_NARROW_PATH/COMMAND/ARGS` 选另一无副作用工具。窄 list 为空会 fail closed,不得用零权限 SK 冒充 scope 收窄。
- `TB_BASE_URL=… TB_SK=… TB_SEARCH_NARROW_SK=… TB_SEARCH_ALLOWED_PREFIX=… pnpm verify:search` — 只读在线 Search验收;目标环境须预置 `日程`/`create document` 在允许前缀内外的 control fixtures,脚本沿 cursor拉全页后判 prefix/subset/strict-shrink。
- `pnpm --filter @tool-bridge/plugin-sdk build && pnpm --filter @tool-bridge/plugin-sdk pack --dry-run --json` — plugin-sdk 发布形状门;该包依赖 pnpm 应用 `publishConfig`,`npm pack --dry-run` 虽可 exit 0 但仍指向 source,不得作为可发布证据。
- `TB_TEST_MCP_URL=http://127.0.0.1:39002/mcp TB_ALLOW_INSECURE_HTTP=true pnpm --filter @tool-bridge/gateway test -- tool.integration.test.ts` — opt-in MCP E2E(先 `ECHO_MCP_PORT=39002 pnpm --filter @tool-bridge/gateway echo-mcp` 起兜底上游)。
- `TB_TEST_LIVE_HTTP=1 pnpm --filter @tool-bridge/gateway test -- tool.integration.test.ts` — opt-in 真实 HTTP 上游(postman-echo)。
- `TB_TEST_S3_* + TB_ALLOW_INSECURE_HTTP=true … context.integration.test.ts` — opt-in s3(本地 s3rver:`pnpm --filter @tool-bridge/gateway s3-mock`)。
- `TB_TEST_SDK_REMOTE=1 pnpm --filter @tool-bridge/sdk test` — opt-in SDK 反向连接生产全链路。

## .env 凭据状态(只记变量名与状态,绝不写值)

| 变量 | 状态 | 备注 |
|---|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | 已配置 | DJJ 账户;验证用 `wrangler whoami`,勿用 `/user/tokens/verify` |
| `CLOUDFLARE_API_TOKEN` | 空缺(注释掉) | 预期内:本地开发靠 wrangler OAuth,CI 时才需要 |
| `TB_DOMAIN` / `TB_BASE_URL` / `TB_NAME_PREFIX` | 已配置 | zone pdjjq.org;当前共享开发 BaseURL;资源命名前缀(=tb) |
| `TB_SECRET_ENCRYPTION_KEY` | 已配置(32B base64url) | SecretStore env-only 信任根;已同步 `wrangler secret put` |
| `TB_SK` | **已配置(= Admin SK)** | CLI/smoke/verify-* 脚本的默认凭证 |
| `TB_TEST_MCP_URL` / `TB_TEST_MCP_BEARER` | 空缺(注释掉) | opt-in MCP 测试用;见下方兜底 |
| `TB_TEST_S3_ENDPOINT` / `_ACCESS_KEY_ID` / `_SECRET_ACCESS_KEY` / `_BUCKET` | 空缺(注释掉) | opt-in s3 测试用;见下方兜底 |
| `TB_R2_ACCESS_KEY_ID` / `TB_R2_SECRET_ACCESS_KEY` | 空缺(注释掉) | `$ref` 预签名用;创建后 presign 主路径可 opt-in 复验 |

## 已知兜底路径(缺外部资源时)

- **真实上游 MCP 空缺** → `pnpm --filter @tool-bridge/gateway echo-mcp` 自建 echo MCP 兜底。
- **外部 S3 空缺** → 本地 s3rver mock opt-in 可重跑;或用 DJJ 账户 R2 的 S3 兼容 API 当"外部 S3"。
- **R2 预签名 AK 空缺** → 大对象走 `/~ref` 网关中转下载路由,功能不缺(生产实测通过)。

## 本机工具链(2026-07-06 核实)

| 工具 | 版本/状态 |
|---|---|
| node | v26.4.0 |
| pnpm | 11.10.0 |
| wrangler | 4.107.0,已 OAuth 登录(可访问 DJJ 与 Lightspeed 两账户) |
| gh | 2.96.0,已登录 Disdjj(有 repo scope,可访问 v1 私有仓库) |
| docker | CLI 29.2.1,守护进程可用(2026-07-08 核实;build/run/restart 镜像验收已过) |

**注意**:wrangler OAuth 下有多账户,所有 wrangler 命令须显式指定账户(`wrangler.jsonc` 已写 `account_id`;脚本内用 `CLOUDFLARE_ACCOUNT_ID`),否则报多账户歧义错误。

## 未竟事项(路线图,非进度账本)

- **工具搜索后续:**实现 semantic并同步外部 HTBP Draft。真实 D1 provision/deploy、admin/narrow `verify:search` 与线上 Dashboard exact-query 已在共享开发环境完成;正式 release 仍须在 PR 合入后从 clean main 重新部署。
- **安全后续:**Secret Reference 使用授权与 SDK/Node canonical-origin 已落地;DO/Node invoke 已重验 scope/registerPaths 与连接代际,Node 有确定性 barrier,共享开发 DO 也完成 155s hibernation与同 ID replacement。仍需设计 HttpOnly session+CSRF/BFF、分布式 rate limit,并保留 KV 最终一致边界。
- **server / app 首发**:`@tool-bridge/server` 0.1.0 与 `@tool-bridge/app` 0.1.0 都待用户手动 `npm publish` 首发 + npmjs.com 配 Trusted Publisher(workflow `publish-server.yml` / `publish-app.yml`);**server 的 npm 安装形态依赖 dashboard 已发布**(regular dependency,dashboard 0.2.0 已在 npm),见 [../guides/npm-publish.md](../guides/npm-publish.md)。
- **tool-bridge-template 外部模板仓库发布**:仓内 `template/` 已有 Deploy to Cloudflare 骨架与 copy-ui;对外模板仍需同步发布。首次请求前必须交互式设置 `TB_BOOTSTRAP_ADMIN_SK`;自定义域须配置 `TB_CANONICAL_ORIGIN`,Preview URLs 保持禁用。
- **`tb init` 向导**:干净账户一条命令拉起(wrangler auth 检查 → provision → 部署 → Admin SK 输出,可重入);当前部署路径是 `pnpm deploy:all`。
- **plugin-feishu 遗留**:list-docs 需 drive:drive 应用权限(用户按需开通)。文档落点已定型团队 wiki(2026-07-08 实测):应用加入知识库成员并授**可编辑**(仅可阅读时 CreateWikiNode 报 131006)后,`create-doc` 传 `wiki_node` URL 建档/append/读回全通;团队 wiki 节点 https://awaken-intelligence.feishu.cn/wiki/AhxNw8VDAi0obtkqioyceAYgnZJ(Tool-Bridge)。可选优化:plugin 加 `FEISHU_DEFAULT_WIKI_NODE` 默认落点,待用户拍板。
- **端到端验收系统化**:Phase 5 A/B/E/C/D 已由本地与共享开发环境证据全部勾选;Compose/browser 最小矩阵尚未固化为长期 Playwright spec。全局 Done 仍需外部 HTBP Draft 与本分支合入 main。
- **中立层后续(2026-08-11 架构评估的 #2–#6,均未授权实施):**①把 13 个只用 `SELF.fetch` 的 gateway 集成测试(5567 行)迁到 Node vitest 直跑 `createTbApp`,让中立层有中立验证面——现有 workerd 绑定几乎不买 CF 保真度(`runInDurableObject`/`listDurableObjectIds`/`env.TB_R2`/`env.ASSETS`/`env.TB_DEVICE` 用量为零,也无 KV 最终一致或 DO hibernation 覆盖);②拆 `tbApp.ts`(~2900 行,全仓最大单文件);③plugin hosted-install 保持 provider 无关;④两个 SearchIndex adapter(D1/SQLite)收敛;⑤部署叙事去 CF 默认(README Deploy 按钮、`provision.mjs` 硬接 wrangler、`wrangler.jsonc` 里提交的真实 account_id/zone/UUID 换占位符);⑥卫生项(`no-explicit-any` 提到 error、zod 3/4 统一)。**已泄漏进协议的 CF 语义**需显式记为约束而非默认:KV 最终一致进了安全模型、D1 Free 50-query 上限塑造了 `TOOL_SEARCH_*` 常量、DO hibernation 把心跳 keepalive 推进了协议。
- **遗留小项**:CLI deviceRuntime 迁移为 SDK 消费者;R2 Access Key 创建后 presign 主路径 opt-in 复验;共享开发环境 `device/demo-mac` 疑似测试残骸待确认清理;dashboard 本地开发流程(vite dev + wrangler dev 联调)下次涉及时补 guide;Cloudflare Analytics 自动 beacon 与严格 CSP 的策略需在平台侧决定。

## 遗留注意

- opt-in MCP E2E 退出码为 0,但 workerd 会打印 SDK sourcemap 诊断与一次 `Network connection lost` 文本,属 harness 噪声,不作为失败依据。
- 本机主 checkout(`~/.superset/projects/tool-bridge`)工作区有一份未提交 WIP(deviceSession→deviceHello 抽取重构);**部署必须从与 origin/main 零差异的干净工作区执行**(2026-07-07 部署即为避开该 WIP 从干净 worktree 出发)。
