# 当前状态(MUST)

> 用途:每次会话开场必读的易变状态快照(部署、代码现状、凭据配置、工具链、未竟事项)。更新时机:部署/凭据/工具链/能力面变化时,由当轮 Agent 更新本文件。最后增量核实:2026-08-18(Cloudflare 首次引导向导、Deploy Button secret 收集与 workers.dev 自动入口;部署/凭据状态仍以各条日期为准)。

## 项目状态

- 2026-08-18:**Cloudflare 首次部署不再要求用户解决“Worker 尚不存在但先要 secret put”的鸡生蛋问题**。①CLI 新增 `tb init cloudflare`：只在 tool-bridge 源码 checkout 内运行，检查/交互登录 Wrangler、选择账户、只读 `secret list` 判断新旧 Worker、幂等 provision、构建 Dashboard、用 `wrangler deploy --secrets-file` 同次注入 trust root 并部署、认证 `GET /~help` 后保存 XDG profile；多账户无交互须 `--account-id`，外部写入须交互确认或 `--yes`。②**秘密边界**：新实例生成 `tbk_`+32-byte base64url Admin SK 与 32-byte SecretStore 主密钥，只经 mode 0600 临时文件传 Wrangler、不进 argv/child env，结束删除；Admin SK 在成功部署后写 mode 0600 profile并仅显示一次。既有 Worker 必须先以对应 profile 验证，绝不生成/覆盖 bootstrap Admin SK；缺加密主密钥时 secrets-file 只含该项。③provision 无 `TB_DOMAIN` 时主动 `workers_dev:true`+清 routes，有 custom domain 时 `workers_dev:false`，不再产“部署成功但无入口”。④`template/.dev.vars.example` 让 Deploy Button 构建前收集两项 secret，`package.json.cloudflare.bindings` 提供生成说明；模板依赖同步到已发布 gateway 0.9/dashboard 0.13 与兼容 Wrangler 4.123/workers-types 5.x。⑤**证据边界**：`pnpm verify` 14 tasks 全绿（package **5533 passed / 7 skipped** + provision/template **3 passed** = **5536 passed / 7 skipped**），`pnpm turbo run build` 7 个发布构建全过；模板另在全新临时目录通过 `npm install`+`tsc`+`wrangler deploy --dry-run`。未执行真实 Cloudflare provision/deploy。CLI manifest/fresh dist/pack 均为 **0.16.0**，版本遗漏检查通过；尚未发布/打 tag，须合 main 后发版。

- 2026-08-17:**Dashboard 与 CLI 已把 `authRef` 从内置集成的用户模型中移除,但保留后端 SecretStore 安全边界**。①Dashboard「添加集成」和高级「挂载节点」共用 `ManagedCredentialFields`,用户只填 API key/OAuth/服务商字段;同 provider/export 替换留空保留现有(含 legacy)绑定,新值自动轮换,`auth:none` 不显示凭证区;Registry 列表/详情不展示内部引用。②CLI `tb integration add` 对应 `--key[-stdin]`/`--field`/`--credential`(复用已保存凭证),0.14 的 `--secret` 与 `rm --purge` 只作隐藏兼容;成功文本、JSON、`integration ls` 都只报 managed/none,不回显槽位或 authRef。底层 `tool/ctx/skill mount --auth-ref` 仍保留给 external/custom/MCP/S3 协议级入口。③**无碰撞与兼容**:CLI/Dashboard 新槽统一为 `integration-v2-${encodeURIComponent(path)}`,所以 `a/b` 与 `a-b` 不再碰撞;已有 Dashboard 节点编辑沿用记录里的旧 authRef,不强迁移。④**安全回滚**:secret set 是 upsert;只有完整 secret 分页确认槽位此前不存在时,挂载失败才删除本轮写入,同名既有或列表未知时不删。凭证仍不进入会回显的 providerConfig。⑤**验收**:CLI **286 tests**、Dashboard **66 tests**定向全绿;`pnpm verify` 最终 14 tasks 全绿(**5529 passed / 7 skipped**),`pnpm turbo run build` 7 个发布构建全过;CLI/Dashboard pack 均含目标版本与 fresh dist。版本已准备:CLI **0.15.0**、Dashboard **0.14.0**。**尚未打 tag、发布或部署**,须 PR 合入 main 后再发版。

- 2026-08-17:**完整 `tb` CLI 已可作为 Bun 独立二进制和 Alpine 镜像分发,不是只拆 Device 子命令**。①`@tool-bridge/cli` 新增 `build:binary`:先由 tsup fresh build npm 的 Node 单文件入口,再由 `scripts/build-binary.sh` 在系统临时目录执行 Bun `--compile --minify`,产当前宿主 `binary/tb`;临时目录规避 macOS 上遗留 60 MiB `.*.bun-build` 的问题,`binary/` 同时被 git/Docker context 排除且不进 npm pack。②根 `Dockerfile.cli` 用 Bun 1.3.14 将 source + core 打入**全部 23 个顶层命令**,只装 CLI/core 的 50 个生产依赖用于 builder;`TARGETARCH` 支持 linux/amd64→`bun-linux-x64-musl`、linux/arm64→`bun-linux-arm64-musl`,`TB_VERSION` 与 CLI manifest 构建期互检。final 是 Alpine 3.22 + ca-certificates/libgcc/libstdc++,固定非 root UID/GID 10001,入口为完整 `tb`、默认 `--help`。③**守护边界**:`tb connect` 仍以前台进程运行并由既有 partysocket + 30s 心跳处理网络闪断/半开连接;Docker `--restart unless-stopped` 或 Kubernetes Pod 负责进程异常退出后的拉起,不在容器里叠第二个 init/daemon。`packages/cli/examples/kubernetes-sidecar.yaml` 给出 Pod UID 唯一 deviceId/path、只读共享卷、Secret profile 与收紧后的 securityContext;`packages/cli/CONTAINER.md` 另覆盖一次性命令、profile volume、Sidecar 文件系统边界和仅复制 musl 二进制的 ABI 要求。④**体积实测**:macOS arm64 单文件 65,526,626 B;Linux arm64 镜像 `docker image inspect` 41,800,531 B、`docker save | gzip` 41,676,964 B(容器内 Bun 二进制约 91.4 MiB);Linux amd64 对应 41,331,507 B / 41,218,659 B。⑤**发布面**:`publish-cli.yml` 的 `cli-v*` 在 npm 之外同步发布 `ghcr.io/tokenrollai/tool-bridge-cli:{version,latest}`(amd64/arm64 + SBOM/provenance);手工 dispatch 默认只推 `edge` 且跳过 npm,供未合 main 的预览验证。当前公开 `edge` manifest digest=`sha256:7a07a18cf8e732f489e016748af6d4d5e76285ef3c76d91cdaa2f5ffa8f47200`,匿名 manifest 200;Actions run `32022150451` 从该次推送 digest 分别拉取两架构,均回报 `0.14.0` 且 `connect --help` 含 `--fs-readonly`。⑥**验收**:`pnpm verify` 14 tasks 全绿(**5518 passed / 7 skipped**,CLI 24 files / 281 tests),`pnpm turbo run build` 7 个发布构建全过;CLI fresh build→Bun binary→`--version`/完整命令矩阵、`pack --dry-run`(确认宿主二进制不泄入 npm)通过。CLI manifest 已 bump **0.14.0**,npm registry `latest=0.13.0`;**正式 `0.14.0`/`latest` 镜像与 npm 均未发布、未打 tag**,须合 main 后再走 release workflow。

- 2026-08-17:**内置 plugin 实现、catalog 与 Dashboard/CLI 挂载表单已按选中的 export 完整对齐,同时封住飞书敏感重定向泄漏**。根因是 `CatalogListItem` 把多 export 的 OAuth/凭证/挂载配置压成 provider 级聚合值,简单向导、高级 MountDialog 与 CLI 各自再猜,而 `system/registry` 又没有统一权威校验。①**契约**:`PluginExport` 新增 `auth:none|single(required/label/description)`,`mountConfigFields` 扩到 context export;plugin-sdk 的 tools/context 都能声明。仓内迁移 provider 的 handler 本就 `requireApiKey`,共用装配器现显式产出 `auth:single(required:true)`;旧第三方 descriptor 缺 `auth` 仍兼容成可选单值。Notes 两个 export 明确 `auth:none` + 可选 `workspace`;PostHog 明确必填 `baseUrl`、可选 `organizationId`。②**catalog**:`system/catalog list` 新增 `exportDetails[exportId]={kind,auth,mountConfigFields…}` 精确真源;旧 `exportKinds/needsOAuth/credentialFields/mountConfigFields` 保留兼容。99 项 codegen 已重求值(40.5 KiB)并通过 `--check`。③**权威校验**:App 在 tools/context 的 registry 写路径统一拒 required providerConfig 缺失/纯空白、必填 authRef 缺失、`auth:none` 误绑 secret、不存在的 secret、OAuth/multi-field 字段不齐;凭证探针只负责验证已提供的凭证,不再被当成“必填”替身。**既有部署升级后行为会变**:新建/改写一个声明必填凭证或配置却缺失的挂载,从“先落一个必然调用失败的节点”改为当场 400;既有已落节点不被迁移或删除。④**管理面**:`tb integration catalog/add` 与 Dashboard `IntegrationDialog`/高级 `MountDialog` 都按选中 export 渲染/校验 auth 与 providerConfig;高级入口也能看到内置 catalog、context export 和 virtualize。新建 secret 后挂载失败时 CLI/Dashboard 只回滚本轮新 secret,复用 secret 不删,不再留孤儿。BuiltinCatalog 逐 export 展示凭证/配置,不再把 Notes 显示成需要 API key。⑤**飞书安全**:`tat.ts`/`feishuMcp.ts` 不再裸 `globalThis.fetch`;敏感请求用 `guardedFetch` 且跨源 redirect 直接拒,覆盖自定义 `X-Lark-MCP-TAT` 头和 307/308 保留的 app_secret body;ESLint 机器禁止 plugin 业务源码新增裸 fetch。⑥**验收**:`pnpm verify` 最终 14 tasks 全绿(core 834 / cli 281 / dashboard 60 / plugin-sdk 38 / plugins 3983 / app 183 / gateway 80(6 skipped) / sdk 19(1 skipped) / server 38,+ provision 2 = **5518 passed / 7 skipped**);`pnpm turbo run build` 7 个可发布构建全过;七包 `pack --dry-run` 均确认新版本与 dist 入口。版本已准备:app 0.5.0 / cli 0.13.0 / dashboard 0.13.0 / gateway 0.9.0 / plugin-sdk 0.5.0 / sdk 0.9.0 / server 0.4.0。**尚未打 tag、发布或部署**;按规则须 PR 合入 main 后再走 release workflow。

- 2026-08-15:**export 可声明 `mountConfigFields`(非凭证挂载配置),catalog 补上"挂载还要配什么"这一维**——[PR #53](https://github.com/TokenRollAI/tool-bridge/pull/53) 已合 main(merge commit `49e5e4b`)并部署到线上,承接 2026-08-14 的 `system/catalog`+`tb integration` 那轮(该批也随本次一并上线 —— 之前只在共享开发环境,digest 从 `8480906` 更新到 `d3887bf`)。**动机**:`system/catalog` 此前只投影凭证字段与 oauth,而 memos/grafana/metabase 这类自建实例 provider 的 baseUrl 需求只写在**插件源码注释**里 —— catalog 看不到,CLI/Dashboard 向导也无从提示,用户挂完要到运行时(或 credentialProbe 报个像凭证问题的错)才发现少配了 baseUrl。①**声明面**:`core/plugin/contract.ts` 的 `PluginExport` 新增 `mountConfigFields?: PluginMountConfigField[]`(key/label/description/required),`~describe` schema 校验(**仅 tools/v1**、字段名不重复,与 credentialFields 同规则),`CatalogListItem` 投影(多 export 取第一个声明了的)。**刻意无 `secret` 字段** —— 值明文进节点记录(`system/registry get` 回显),给它"遮蔽输入"会诱导把密钥放进这条不加密的通道;密钥永远走 `credentialFields`。`required` 缺省视为**非必填**(与 providerConfig "有就用、没有走默认" 一致,方向与凭证字段相反)。②**plugin-sdk**:`ToolsExport.mountConfig()` setter,与 `credentials()`/`oauth()` **不互斥**(一个 export 可以既要 API key 又要 baseUrl);`createProviderPlugin` 接线 `mountConfigFields?` 入参。③**6 个 provider 声明**:memos/grafana/metabase 必配、outline/langsmith(region+workspaceId)/railway 可选;`catalog.generated.ts` 重新求值(35.9→36.7 KiB)。④**CLI**:`tb integration add` 挂载前校验必填配置缺失(`assertMountConfig`),`tb integration catalog` 列表加 CONFIG 列(带 `*` 标必填)。**同轮修两个真 bug**:(a) 顺序缺陷——`assertMountConfig` 与 providerConfig 解析原在 secret 代建**之后**,缺 baseUrl 会留下孤儿 secret,已把校验提到任何写操作之前(与 `assertFieldNames` 一致);(b) 挂载 kind 静默挂错——跨 kind 多 export 的 provider(notes:actions=tool / documents=context)选 context export 时,nodeKind 从 `nodeKinds=['context','tool']` 落到默认 `'tool'`、挂载被平台拒且无参数可救,已让 core `CatalogListItem` 投影新增 `exportKinds`(export id → kind)、CLI/Dashboard 按选中 export 取 kind(退化:选中 export 的 kind → 单一 nodeKind → `'tool'` 兜底)。⑤**Dashboard**:`IntegrationDialog` 第三段"配置"在 catalog 声明了 `mountConfigFields` 时渲染**带标签的字段输入**(必填标 *、显示 description),未声明才退回自由 k=v(external plugin 兜底);`buildIntegrationCalls` 校验必填配置;选 provider 时**预填默认挂载路径**(`defaultMountPath`:tool→`tools/<id>`、context→`notes/<id>`,可改)并清 config 残留;`BuiltinCatalog` 每行加"添加"按钮(`IntegrationDialog` 新增 `defaultProvider` prop,预选该 provider 打开向导)+ "配置"列;Overview 快捷入口首位加"集成目录"(此前想加集成的用户第一步无指引)。⑥**验证**:`pnpm verify` 全绿(core 832 / plugin-sdk 37 / cli 278 / plugins 3980 / dashboard 54 / app 180 / gateway 80(6 skipped)/ sdk 19(1 skipped)/ server 38),`pnpm turbo run build` 7 包过。**已部署到线上**(2026-08-15,gateway Worker version `95b6f4cd-f599-47ea-942b-d3aabfa824b9`,`pnpm deploy:all` 从合 main 后零差异工作区):线上 `/healthz` catalog digest = `d3887bf`(与本地一致)、`system/catalog` 实测透出 memos 的 `mountConfigFields`(baseUrl required) 与 `exportKinds`、smoke 4 项过、Dashboard `/ui/` 入口 HTML SHA-256 = `7e32756…`(本地=生产=SPA 深链接三者一致);gateway 运行时 `/healthz.version` 仍 0.8.0(Worker 运行时版本,与 npm 包版本归属不同)。**npm 版本仍未 bump**(部署走 Cloudflare Worker,不碰 npm 发布 —— 两条独立发布面):改了外部契约的 public 包(plugin-sdk `mountConfig`、cli、dashboard,及 `system/catalog` 响应新增字段的 gateway/sdk/server)待跑 `pnpm release:plan` 用真实 registry 核对后走 `release.yml` 一次发完。**组件层交互(行内添加按钮、预选、字段渲染)尚无组件测试,待真实浏览器核对**;`integrationPlan`/`defaultMountPath`/`assertMountConfig` 的纯逻辑有单测。**遗留**:`mountConfigSchema`(P3,`PluginPackage` 上的 JSON Schema 版)与本轮的 `mountConfigFields`(export 上的扁平字段)是**两个不同层**——前者是插件包安装分发单位的配置,后者是 export 挂载配置;本轮只做了后者,前者的消费面仍未落地。

- 2026-08-14:**内置插件目录改成编译期 catalog,读路径结构上写不了库;`system/catalog` + `tb integration` 把"挂一个集成"从四个概念收成一条命令**——决策与求值前提见 [../memory/decisions/builtin-catalog-not-registry.md](../memory/decisions/builtin-catalog-not-registry.md)。①**catalog codegen**:`packages/plugins/scripts/generateCatalog.ts` 构建期逐个 import 内置插件、真调 `fetch('/~describe')` **求值**(不解析源码 —— descriptor 是 `createPlugin()` 链式调用的运行时产物,静态解析等于把 plugin-sdk 的构造语义抄第二遍),产出 `packages/plugins/src/catalog.generated.ts`(99 个插件 / 35.9 KiB,每条一行便于 review,含 per-entry digest 与目录级 `BUILTIN_CATALOG_DIGEST`)。命令 `pnpm --filter @tool-bridge/plugins generate:catalog [--check]`;闸门是 `test/runtime/catalogCodegen.test.ts`(逐条对拍求值 digest,**放 test 而不是 build** —— `pnpm verify` 比发布 workflow 的 build 早得多,漏到打 tag 之后返工要删 tag 重打)。**刻意不收 action 表**:求值产物 2.49 MiB,与 `src/<name>/schema.ts` 是同一份数据的第二个副本,而工具表运行时本就走 `toolcache:<path>`;收进来只让仓库与 Worker bundle 各多背 2.5 MiB,且改一行 description 就翻 digest。求值前提实测:`~describe` **99/99 成功、零网络零凭证零 env**;**`dynamic: true` 不在 `~describe` 里**(plugin-sdk 只在 `help()` 输出,`describe()` 刻意让 proxyTools 与静态 tools 对外同形状),故 codegen 无法从 descriptor 判断哪个 export 可静态化。②**A1「删除即复活」根除**:新增 `packages/core/src/plugin/catalog.ts`(`BuiltinCatalog`/`BuiltinCatalogEntry` + `resolveBuiltinExport`(**无 store 参数**)/`resolveExternalExport`/`resolveIntegration`(唯一分发口;优先级按 **endpoint 决定契约真源**:`https:` 注册记录赢内置项(显式注册该赢过平台自带的),但 `binding:` 注册记录**输给**内置目录 —— 它指向的就是本进程这份代码,而那种记录的 `pluginmeta:` 快照只在注册那刻抓过一次,让它赢等于让 A3「契约永久陈旧」在被存量自动补注册污染过的部署里原样复活;目录里没有该 id 时仍走注册记录)+ `canonicalCatalogJson`/`catalogDigest`/`catalogSetDigest`),关键是它只吃 `ReadOnlyStore`(**只有 `get`**),**结构上写不了库**,而不是"约定读路径要传裸 store"。`packages/app/src/toolNodes.ts` 的 `autoRegisterBinding` 整个删掉:它存在的唯一理由是"describe 缓存在 KV 里",catalog 把那个前提消掉了;原先 7 个调用点里 4 个传 `deps`(会写库)3 个传裸 `store`(不写)的不对称随之消失(此前 tool 侧 help/call 都复活、context 侧 help 复活但 call 报"未知 provider" —— 同一语义在四条链上有四种行为)。**digest 分两级**是有意的:单级全量 digest 会让任一 provider 的文案改动触发全局红灯,"三宿主一致"退化成噪声。③**A4 修复:部署形态不再改变产品能力**——此前唯一装配点是 `gateway/src/deployEntry.ts`,`packages/server` 连 `@tool-bridge/plugins` 依赖都没有,于是同一份代码在 Workers 上 99 个 provider、在官方 Node/Docker 镜像上是 **0 个**;现 `packages/server/src/main.ts` 装配全量内置目录(+ `@tool-bridge/plugins` devDep + tsup `noExternal`)。副作用是 server tsconfig 要补 `lib: ["ES2023","DOM","DOM.Iterable"]` —— 它现在编译插件源码,而那些代码按 `tsconfig.plugin.json` 只依赖 Web 标准全局(`BodyInit`/`Headers`),gateway 靠 workers-types 拿到,Node 侧只能由 lib 补齐(`types: ["node"]` 未动,不是放宽)。`packages/plugins` 同时拆出 `tsconfig.node.json` 给 Node-only 的 `scripts/`,`tsconfig.json` 只管 `src`/`test` 保持宿主中立(否则整包开 `types: ["node"]` 会让插件实现里误用 `process.env` 静态通过),typecheck 跑两个 project。注入面三宿主全接:`TbAppDeps.pluginCatalog`、gateway `createApp({pluginCatalog})`、server `ServerConfig.pluginCatalog`、sdk `ToolBridgeConfig.pluginCatalog`;catalog 与 bindings **是一对**(catalog 说"声明了什么",bindings 说"代码在哪"),只给 bindings 则插件调得动但解析不出 export,该失配已被 app 集成测试钉住。④**新 builtin `system/catalog`**(`packages/core/src/builtin/catalog.ts`,`BUILTIN_MODULES` 七个变八个,`buildDeps` 注入,`BuiltinAssemblyOpts.pluginCatalog`):cmd `list`/`get`/`search` **全部 scope=read 而非 admin** —— descriptor 里没有敏感信息(只有 provider 名、export id、凭证**字段名**),而挂载只要 register scope,浏览不该更严,否则渐进式发现在这条路上断掉;`list`/`search` 回 `CatalogListItem` 投影(id/digest/exports/nodeKinds/needsOAuth/description/credentialFields),`get` 才回 describe 全文;分页 cursor = 下一条 id,**失效 cursor 从头开始而不是报错**(目录是派生视图)。`system/plugin` 同步收窄为 **external-only**:内置插件不落库,`plugin list` 只列 external,`plugin catalog` 里内置项恒 `registered: false`——那不是"没配好",是"不需要"。⑤**`tb integration` 命令族**(`packages/cli/src/commands/integration.ts`,CLI 顶层命令族 22 → 23):`catalog`(--search)/`add`/`auth`/`ls`/`rm`(--purge)。`add` 是**编排不是新协议**(底下仍是 `system/secret` 与 `~register`):secret set → mount →(oauth 时)提示授权;凭证四种给法互斥 `--key`/`--key-stdin`/`--field k=v`/`--secret <name>`,**secret 名由挂载路径派生**(`integration-<path>`)—— 不再让用户手打一个两处都要对上的自由文本(拼错不报错,agent 首次调用才 401);五种错本地就拒(字段名拼错/缺字段/多字段却给单值/多 export 未指定/export 不存在),而 catalog 查不到或无 read 权限时**降级为不给提示、不阻断挂载**(catalog 只覆盖内置,external plugin 不在里面)。`auth` 直接复用 `toolAuthCommand()` **本体**(同一个 Command 挂两处):那条链有 `--local` 与 redirect 降级,抄一遍必然漂移。`ls` 是 `system/registry list` 的投影,不造第二真源 —— 一个 instance = 一次挂载,身份 = 节点 path。另补 `tb tool mount --config k=v` 与 `tb ctx mount --config k=v`(共享 helper `packages/cli/src/registry.ts` 的 `parseConfigSpecs`,值一律按字符串收,不猜类型),这是 providerConfig 的输入口 —— 此前 CLI 与 Dashboard 都没有,memos/grafana/metabase/langsmith 这类"必须配 baseUrl"的 provider 只能手写节点 JSON 直打 `system/registry`,那是管理旁路。⑥**Dashboard**:`useIntegrationCatalog()` 查 `system/catalog`(`staleTime` 5min,descriptor 是编译期常量),新增 `pages/system/forms/integrationPlan.ts`(纯逻辑,可被 Node vitest 直接断言)+ `IntegrationDialog.tsx`(catalog 驱动的向导);`BuiltinCatalog.tsx` 重写,**去掉"注册"按钮**并把数据源从 `system/plugin catalog`(admin)换成 `system/catalog`(read);RegistryPage 主按钮改「添加集成」,「挂载节点」降 outline。**2026-08-14 同日修一个真 bug(B1-X)**:`credentialPlanFor` 此前按 `secret === false` 把凭证字段分流进 providerConfig,而运行时 `assertToolConfig` 把整个 `credentialFields` 交给 core `parseCredentialValues`,后者要求每个 `required !== false` 的字段都在 authRef 解出的 JSON 里 —— 照分流后的引导操作**挂载必被拒**,精确影响 8 个声明了 `secret: false` 的 provider(confluence/ghost/jira/mattermost/shopify/twilio/upstash_redis/wordpress);`secret` 是展示语义(要不要遮蔽输入)不是通道语义,非凭证的挂载配置该由 export 独立声明(等 `mountConfigSchema` 消费面落地)。`PluginCredentialField.label` 同时改回可选 —— dashboard 手抄成必填,与 core 声明漂移。⑦**发布流程改成一轮一次 dispatch**(用户明确要求"不要每次打 N 个 tag"):`scripts/release-plan.mjs`(**纯读**)+ `pnpm release:plan` 列 local vs registry latest、拓扑排序发布顺序、检"发了会装不上"的阻塞项;`.github/workflows/release.yml` 一次 `gh workflow run release.yml -f dry-run=false` 发完,**只编排不自己发**(npm Trusted Publishing 的信任按 workflow 文件名授予,搬进来等于 7 个包重配 Trusted Publisher)、串行等每个 publish 真正跑完、拒非 main、tag 已存在则跳过、`-f packages=a,b` 发子集;`scripts/check-version-bumps.mjs` 进 CI 当 PR 提醒(**warning 不拦** —— 该 bump 哪些包是 public artifact ownership 的判断,机器判不了)。⑧**验证**:`pnpm verify` 14 任务全绿(core 827 / plugins 3980 / cli 274 / app 163 / gateway 76(6 skipped)/ server 38 / sdk 19(1 skipped)/ plugin-sdk 33 / dashboard 41 = 包测试 5451,+ provision 2 = **5453 passed / 7 skipped**),`pnpm turbo run build` 7 包过。**真实 Node 宿主端到端实测**(server `dist/main.js`,端口 39120/39121):`system/catalog` help 三 cmd 均 scope=read,list/search/get 正常(jira 回两个 credentialFields、sentry `needsOAuth=true`);`tb integration add tools/amap --provider amap --key-stdin` 一条命令建 secret `integration-tools-amap` + 挂载,`tb help tools/amap` 可列工具;`tb integration ls` 正确投影;`rm --purge` 同删节点与派生 secret;三种字段错误全部本地拒;**tavily 挂载被 credentialProbe 用假 key 正确拒绝**(证明探针在挂载时生效)。A1 回归实测:挂载 + 读一轮后 `plugin list` 为空、`plugin get` 404,卸载后 `~help` 404 **不复活**;`tools/tavily` 无 register 直接挂载成功。**版本**:app 0.4.0 / cli 0.11.0 / dashboard 0.11.0 / gateway 0.8.0 / sdk 0.8.0 / server 0.3.0(plugin-sdk 0.4.0 于 2026-08-14 未动)。
- 2026-08-12:**`tbApp.ts` 拆分完成(架构评估 #2 的最后一项)**——2948 行的全仓最大单文件降到 144 行,只剩装配:构造 Hono、建 `RouteEnv`,按序挂安全头中间件 → 树外免认证路由 → 认证中间件 → `/~mcp`/`/~search`/设备 WS → GET/DELETE/POST 三个通配分派 → notFound/onError,**路由顺序即安全语义,现在一屏读完**。拆出 8 个纯函数模块(`deps`/`responses`/`paths`/`federation`/`deviceNodes`/`toolNodes`/`contextNodes`/`helpModel`)与 9 个路由模块(`routes/{publicRoutes,mcp,search,tree,help,describe,feedback,register,invoke}`)+ 共享的 `routes/env.ts`,最大单文件 482 行。**纯移动重构**:handler 体逐行搬迁,唯一的语义改动是把闭包捕获的 `deps`/`searchSync`/`builtinsOf`/`globalSearchCapabilities` 收进显式的 `RouteEnv`,handler 签名统一成 `(c, env)`;`index.ts` 公开导出名不变,三个宿主包零改动。校验用了"行集合 diff":旧文件里消失的行全部是加了 `export` 的声明行与加了 `env` 的调用点,零逻辑体丢失。`pnpm verify` 全绿(app 121 / gateway 76 / server 38)。落点见 [../architecture/code-map.md](../architecture/code-map.md)。
- 2026-08-12:**内置插件目录按价值策展并迁完策展名单,补上 oauth 声明面**——前三批迁移按"流程能否规模化"选批(刻意不看 provider 有没有人用),流水线因此得到验证,代价是 114 个产物里大量是无使用场景的冷门 SaaS。本轮反向按价值筛:删 74 个、保留 40 个,再从上游选 57 个高价值 provider 迁入,**97 个产物 / 1275 action 全部落地**,内置目录 99 个 loader。选批不是拍脑袋:对 121 个候选跑全量探针(1949 action / 1827 等价 = 93.8%)后取价值 ∩ 可行性。凭证形态(经 `~describe` 实测,非静态 grep):单值 api_key 82、`credentialFields` 多字段 10、`oauth` 托管授权码 5(sentry/gmail/googlecalendar/googledocs/dropbox)、`credentialProbe` 66,互斥零违反。规模最大三个:github(145 action,api.ts 按上游 runtime-* 拆 6 文件)、gmail(46)、googlecalendar(37)。**同轮发现并修掉两个真缺陷**:①`plugin-sdk` 的 `ToolsExport` 上没有 `oauth()` 声明面 —— `app/src/providerOAuth.ts` 的托管授权码流程早已完整且有 16 个端到端集成测试,却没有任何插件能触发它,连那些测试都是手写 `~describe` 绕过 SDK 的(`MIGRATION.md` 里"oauth2 待平台侧补"那句因此过时,已标注);已补上并在 SDK 侧当场拒三处互斥组合,**plugin-sdk 0.2.0 → 0.3.0(public 包新增能力,产物入口已验证;未发布未打 tag)**。②core 的 `toToolResult` 只看"有没有 `content` 键"就把对象当成已成形信封透传,而 `content` 在业务出参里极常见(GitHub reaction `{id,content:'+1',user}`)—— 顶层其余字段静默丢失,**不报错不掉测试**,实测 11 处出参声明踩到(6 个 provider,含已合并的 anthropic/posthog/tavily);判据改成"键集合不含外来键"(ToolResult 是封闭形状)。四条可复发教训已进 guide:批量 fan-out 要可断点续跑(第一轮 13 个 subagent 因连接中断与周额度耗尽全退,未完成产物连同指纹撤到暂存区、第二轮零重复劳动)、wire 测试绿 ≠ 类型对(`compact()` 类型缺陷累计四次,tsc 才是判据、grep 会误判 29 处)、`new Response('', {status:204})` 在 undici 下 TypeError 而异常被归一成 internal 500、修完缺陷要回头改因它而写的注释。闸门:lint、typecheck 9 包、test 14 任务 5340 例、build 7 包全过。
- 2026-08-12:**plugin 契约扩了三个能力,并从 open-connector 迁入 114 个 provider(806 actions)**——契约面新增 `credentialFields`(多字段凭证,对应上游 custom_credential)、`credentialProbe`(挂载时用真实调用验凭证可用)、`oauth`(provider 型托管授权码流程,与 mcp 那条**共用 state 密封与回调页、流程分开**:那条靠 MCP SDK 的 discovery+DCR,这条端点来自 `~describe`、client 由用户自注册;令牌存 `puoauth:token:<path>`)。CLI 补 `tb secret set --field k=v`。迁移侧建成流水线(`packages/plugins/scripts/migrate/`:求值上游定义 → JSON Schema→Zod codegen → 人工机械改写业务逻辑)与三道回归闸门(等价/形状/wire),内置目录现 116 个插件。**同轮做了一次设计 review 并修掉七处**:插件 env 收窄成白名单(此前 `Record<string,string|undefined>` + 注释"Node 宿主常用 process.env",一行 `ctx.env.TB_SECRET_ENCRYPTION_KEY` 即拿主密钥;生产宿主尚未接线故是设计缺口而非已发生泄漏)、跨源重定向剥凭证头(逐跳校验了"跳到哪"没管"带什么去",33 个产物凭证在 header)、`EgressBlockedError` 继承 TBError(裸 Error 会让 SSRF 拦截呈现为 internal 500)、未配 `PLUGIN_TOKEN` 改 fail closed(此前只要求 Bearer 非空)、平台从 List 核验探针形状(只读+无必填入参)、`binding:` 传输补 30s 超时(此前只有网络分支有,而 114 个产物走 binding)、上游 401 触发 OAuth 强制刷新重试(此前"靠 401 触发"这句注释在 provider 这条路上无对应实现,不返回 `expires_in` 的 provider 令牌失效即永久坏死)。方法与判据见 [../guides/plugin-design-and-migration.md](../guides/plugin-design-and-migration.md);**未修项(B7/B8/B10 与 C 类六条)登记在 `packages/plugins/MIGRATION.md`**,其中 B10(dedupe 键不含挂载/凭证指纹)最便宜。
- 2026-08-12:**架构评估 #3–#6 的其余四项一并落地**——③**plugin 托管形态去 CF 默认**:`packages/plugins/README.md` 把三形态(进程内 `binding:` 直调 / Node 进程外挂 HTTP / Workers 独立部署)写成并列契约,CF 自动部署降为其中一条路径而非默认;`wrangler.feishu.jsonc` 去掉 `account_id` 改读 `CLOUDFLARE_ACCOUNT_ID`,`deploy:feishu` → `deploy:feishu:cf` 并补 `serve` 脚本。④**两个 SQL SearchIndex adapter 收敛**:D1(421 行)与 SQLite(359 行)此前 >85% 逐行重复,`shortTermSql.ts` 只收了最容易出事的一小段;新增 core `search/sqlSearchIndex.ts`(477 行)持有全部驱动无关逻辑(schema DDL、查询 SQL、material-change 判定、容量归一、cursor/分页),宿主差异压成 `SqlSearchDriver` 的 `all/first/write/insertRecords/insertSnapshots` + 可选 `assertInsertBudget`;**core 只产出 SQL 文本与参数数组,不认识 D1Database 也不认识 better-sqlite3**;adapter 各剩驱动(D1 115 行含 JSON1 攒块与 50 查询预算,SQLite 82 行同步 API 包 Promise + transaction)。⑤**部署叙事去 CF 化**:`wrangler.jsonc` 去账户化(account_id / routes / TB_CANONICAL_ORIGIN / TB_R2_S3_ENDPOINT 不再入库,KV/D1 id 换零占位),`provision.mjs` 扩成账户特定配置的**唯一 materializer**(除 KV/R2/D1 幂等创建与 id 回填外,把 .env 的 account/domain/base-url 与前缀派生的资源名一并写进配置,缺变量就跳过并说清后果——**前缀派生的 bucket_name/database_name 此前不跟随 `TB_NAME_PREFIX`,改前缀会 deploy 到不存在的资源**),provision 测试改 hermetic(`TB_PROVISION_ENV_FILE` 指 fixture,不受开发机 .env 影响);README 双语把两个宿主适配器并列成能力对照表并写明 provision 是 CF-only 可选步骤。⑥**卫生项**:`catalogs.zod4` 分叉的理由已不成立(MCP SDK 1.29 peer 是 `^3.25 || ^4.0`),catalog 单条目升 `^4.2`、app/gateway 改回 `catalog:`、core 六个 schema 迁 zod4 原生写法(`z.record` 双参、`z.iso.datetime()`、去掉 enum tuple cast、`.refine` 的 `message`→`error`);`no-explicit-any` 当前零违规,提到 error 守住状态。
- 2026-08-11:**中立层的验证面迁到中立宿主**(架构评估建议 #2)——`packages/app` 新增普通 Node vitest(`vitest.config.ts` + `test/harness.ts`),13 个测试文件从 gateway 的 workerd 池搬过来:其中 4 个(search/searchSynchronizer/pluginBinding/toolCache)本来就是纯 Node 写法、只是恰好跑在 workerd 里,零改动搬迁;另 9 个只经 `SELF.fetch` 驱动,机械换成 `app.request()` 直打 `createTbApp`。**中立性从此是被执行验证的,不只是被 `types: []` 静态约束的**——树里若混进宿主专属全局,这套在 Node 下直接炸;`test/` 也纳入 `types: []`,测试同样不得用 Node 专属 API。迁移暴露三处装配缺口(全在 harness 侧,不是树的 bug):`/~mcp` 只在宿主注入 SearchIndex 时才投影 `tb_search`、`/~search` 缺索引则 404,故新增 `test/memorySearchIndex.ts`(真 `MutableSearchIndex`,复用 core 的序列化/digest/query 预处理/cursor 加解密,只把全文匹配换子串——**顺带是第三个 SearchIndex 参考实现,证明该注入点不绑 SQL 更不绑 CF**);OAuth redirect_uri 断言原先硬编码 `wrangler.jsonc` 里的生产域名,改用 fixture origin,测试不再依赖部署配置。分布:app 12 文件 121 例约 1.2s / gateway 6 文件 76 例(同批用例在 workerd 下要 6.8s)。留 workerd 的是真吃 CF 的部分:DO WS hibernation、设备 WebSocket、真实 D1、Static Assets,以及 `tool`/`context` 里靠 env binding 开关的 opt-in 路径。gateway 随测试摘掉 `@tool-bridge/plugins` 与 v1 MCP SDK 两个 devDep;`publish-{gateway,sdk,server}.yml` 的 test 闸门都加上 app(三者产物都含中立层),`publish-app.yml` 改用 app 自己的套件。**另修:`publish-app.yml` 上一轮被误写进主仓库工作树而非本 worktree,从未进过提交,本轮补上。** `pnpm verify` 14 任务全绿。
- 2026-08-11:**宿主中立层抽成独立包 `@tool-bridge/app`,gateway 降为薄 CF adapter**(架构评估建议 #1 的彻底版)——此前 tbApp/bootstrap/deviceHello/providers 这套宿主中立代码住在 gateway 里,sdk 与 server 跨过 exports 深链 `@tool-bridge/gateway/tbApp` 消费;包名/description/keywords 全说 cloudflare,gateway tsconfig 又全局挂 `types: ["@cloudflare/workers-types"]`,中立性只靠人工纪律。现搬 17 个文件(~6050 行)到 `packages/app`,其 tsconfig 刻意 `types: []` + `lib: ["ES2023","DOM"]`,**中立性改由类型系统保证**;gateway 只剩 ~1212 行真正的 CF 绑定(app/deviceSession/kvStateStore/r2Object/d1SearchIndex),exports 收回只剩 `.`。三个宿主包一律 devDependencies + `noExternal`(**core 是 private 包各自 bundle,app 若留 external 会并存两份 core,`err instanceof TBError` 跨副本恒 false,TBError 被静默降级成 internal;workspace 内跑源码单副本测不出**),三个 `tsconfig.build.json` 同步补 app 的 paths 映射(否则 rollup-dts 解析不到源码,d.ts 留悬空 import 而 app 不随发布走)。搬迁只暴露一个真 bug:`objectBodyToBytes` 返回 `Uint8Array<ArrayBufferLike>`,标准 `BodyInit` 拒收 shared-buffer 支撑视图(workers-types 恰好宽松放过),已在 core 修。产物对照 HEAD 实测基线零回归(gateway 452.43→452.66 KB,sdk 2.27 MB / server chunk 1.92 MB 持平,新增 app 403.67 KB);`pnpm verify` 13 任务全绿(**1291 passed / 7 skipped** + provision)。新增 `publish-app.yml`,三个发布 workflow 的 typecheck `--filter` 链改指 app。搬迁时刻意不动测试(单步失败面小);验证面迁移见下一条(评估建议 #2,已完成)。**app 0.1.0 当轮未首发、未部署**(首发已于 2026-08-12 完成,见下方「npm 已发布」条)。
- 2026-08-11:**内置插件重构为"单包源码文件夹"形态**(用户对照 open-connector 拍板)——废除"每插件一个 workspace 包"(package.json/tsconfig/vitest/wrangler/node_modules 全套壳),新建 `packages/plugins`(private):`src/<name>/` 每插件一个纯源码文件夹(现有 `src/notes/`、`src/feishu/`),`src/registry.ts` 是懒加载装配表(`BUILTIN_PLUGIN_LOADERS` + `builtinPluginBindings(env,{include})`,include 供 CF 按体积裁剪;规模大后改 codegen,对齐 open-connector 的 registry.generated 形态)。`plugins/*` workspace 目录已删除(workspace glob/Dockerfile COPY/根 filter 同步);feishu 独立 Worker 部署保留为 `wrangler.feishu.jsonc`(`pnpm --filter @tool-bridge/plugins deploy:feishu`);compose plugin 服务改跑 `packages/plugins/scripts/serve.ts`(@hono/node-server 起外挂 HTTP 形态,env 传参,已本地实跑探活 healthz/~describe);feishu 的 9 例 workerd 测试移植为纯 Node vitest(SELF.fetch→plugin.fetch shim,断言零改动全过);gateway 测试改 import `@tool-bridge/plugins/notes`。全量验证 13 任务全绿。**注意:compose 三跳栈尚未整栈重跑(仅 serve 进程级探活);生产 tb-plugin-feishu Worker 未重部署,部署入口已迁移。**
- 2026-08-11:**system/plugin `catalog` cmd + `tb plugin catalog` 已落地**(可用目录列举,CLI 同轮对等)——admin cmd 列出宿主装配的进程内插件(`bindings` 经 `BuiltinAssemblyOpts` 从 `pluginBindings` 接线),逐项对拍注册状态(registered/pluginId);binding 集成测试断言 available→registered 翻转,core/gateway 的 cmd 表钉住测试与 CLI strictParsing 叶子清单同步,协议契约文档已更新。全量验证 13 任务全绿。**后续**:构建期插件集合选择、plugins/ registry codegen 与脚手架、Dashboard Plugins 页 catalog 集成(可选)。
- 2026-08-11:**binding: 进程内插件传输已落地**(plugin-in-process-catalog 决策第一刀)——`TbAppDeps.pluginBindings`(binding 名→fetch handler,懒加载放 handler 闭包)注入后,`binding:<name>` manifest 的探活/`~describe` 契约抓取/envelope 调用全部进程内直调(此前 501),wire 契约与 HTTP 通道逐字节一致;未装配 binding fail closed(unavailable);外挂 https plugin 通道不变。集成测试把全局 fetch 打桩为抛错,证明注册→挂载→调用零网络出站(gateway 197 tests)。三宿主装配入口已铺通:`createApp(opts.pluginBindings)`(Workers)/ `createTbServer(config.pluginBindings)`(Node)/ `createToolBridge(config.pluginBindings)`(SDK),类型自 gateway 根与 tbApp 子导出。**后续**:builtin system/plugin 的"可用目录"列举(须 CLI 同轮对等)、构建期插件集合选择(CF 体积上限)、plugins/ 目录 registry codegen 与脚手架。
- 2026-08-11:**workspace 规模化基建第二步 + 手写代码收敛已落地**:①引入 **Turborepo**(`turbo.json`;所有包互相消费 TS 源码,故任务经 `dependsOn: ["^typecheck"]` 把上游文件哈希纳入缓存键,已用探针证实改 core 打掉全部下游 test 缓存而无关包命中;gateway 补 `@tool-bridge/dashboard` devDep 声明 ui.integration 的真实测试输入边),根 `typecheck`/`test` 走 turbo,热 verify 从分钟级降到秒级,pre-commit 全仓 typecheck 自动享受缓存;②`provision.mjs`:R2 存在性探测改 `r2 bucket info --json`(缺失 bucket 非零退出,已对真实 API 实测),`wrangler.jsonc` 回填改 **jsonc-parser** modify/applyEdits,fake-wrangler 回归已同步,真实幂等 provision 复跑全 skip 且零 diff;③core 三份 base64url 合一至 `encoding/base64url.ts`(统一严格语义:字符集+len%4≠1;旧宽松累加器会把非法长度静默解成截断字节再喂 AES-GCM/HMAC),secretStore 重导出保持公开面,新增往返/拒绝钉住测试;④CLI `parseIsoTimestamp` 委托 core `normalizeExpiresAt`(zod datetime 正则本身日历感知);⑤`matchGlob` 相邻 `**` 合并消除指数回溯 + >64 段 pattern fail closed(钉住测试);⑥短词 escaped-LIKE SQL 三件套抽到 core `search/shortTermSql.ts`,D1/SQLite 两 adapter 共享唯一真源。root devDeps 新增 turbo/jsonc-parser。全量验证:typecheck+lint+13 turbo test 任务+provision 全绿 exit 0(core 751)。**遗留**:skillhub frontmatter 换 `yaml` 需对"core 只依赖 zod"纪律做显式破例决策,未动。
- 2026-08-11:**workspace 规模化基建第一步已落地**(为"上千 plugin"做准备):①插件迁出 `packages/*` 到独立 `plugins/*` workspace 目录(`packages/plugins/src/notes`、`packages/plugins/src/feishu`;npm 包名不变),根 `test:integration` 改按目录 glob `--filter './plugins/*' --if-present` 选插件,Dockerfile 增 `COPY plugins`、compose plugin 服务路径同步;②共享依赖(≥2 包使用)版本收编进 `pnpm-workspace.yaml` catalog(gateway 的 zod 4 单列命名 catalog `zod4`,与 core 系 zod 3 的语义统一是未竟独立事项;dashboard `@types/node` 24→26 对齐),已实证 `pnpm pack` 会把 `catalog:` 改写为真实版本号(cli tarball 核对);③新增根 `tsconfig.plugin.json`(ES2023+DOM、无 Node/Workers 全局,与 plugin-sdk 宿主中立姿势一致),两插件 tsconfig 从两种漂移姿势统一为一行 extends;④新增 `.github/workflows/ci.yml`(main push + PR 跑 `pnpm verify`)并补根 `packageManager: pnpm@11.21.0`。每步后 `pnpm verify` 全绿(catalog 步实跑 exit 0:745+10+242+22+19+9+194+38 + provision)。既有 wrangler↔workers-types 的 peer 警告为存量,与本轮无关。后续步骤(插件脚手架、plugin-sdk Node serve 层与 multi-plugin host)见本轮评估,未实施。
- 2026-08-11:**共享开发环境已部署并完成 Phase 5 在线验收,但不是 production release**——用户明确项目仍在开发中并授权覆盖既有开发实例。真实 APAC D1 `tb-search` UUID=`f788b779-ec1c-4fba-ac1f-b780fab990fc`,二次 provision 精确 skip;plugin-feishu version=`140c2a97-3fbe-4faf-8e8a-7a7045606e67`,gateway version=`ce61da86-fb00-4550-915d-08eb4c7a7c94`。`pnpm smoke`、线上 MCP admin/narrow list+call(29→2)、D1 Search 两 query admin/narrow(2→1)、标准与 155s hibernation device、plugin/v2 飞书 create/fetch/update、窄 SK Dashboard exact-query DOM 均通过;临时 SK/registry/D1 fixture 已清理,飞书验收文档保留。线上 Dashboard console 会记录 Cloudflare 自动注入 Analytics beacon 被 `script-src 'self'` CSP 拦截,不得写成 console 0,也不应为消警放宽 CSP。[PR #35](https://github.com/TokenRollAI/tool-bridge/pull/35) 已合入 `main`;全局 Done 仅继续受外部 HTBP Draft 同步阻断。
- 2026-08-11:**SearchIndex v3 与 MCP 发现面三工具已部署到共享开发环境**——gateway version=`2221abb7-bd12-4c00-8bc2-8918d5bae9b4`;D1/SQLite 仅保存轻量 `path/name/description/feedback/toolDigest`,不再持久化完整 ToolSpec,网关在权限裁剪后从 canonical registry/tool cache 水合完整结果。无状态 `/~mcp` 新增稳定 `tb_search`、`tb_help`、`tb_list_nodes`,分别复用 `/~search`、`~help`、`~tree` 的现有认证与权限链。部署后 `pnpm smoke` 通过;真实 D1 v3 重建出 8 个飞书工具;API、真实 CLI 与 Dashboard DOM 均命中 `feishu:create-doc`,返回完整 8790-byte description;官方 MCP SDK `tools/list` 共 38 项,三个稳定工具均完成真实 call。线上 Dashboard 仍只有已知 Cloudflare Analytics CSP error。共享 adapter、D1/SQLite、官方 MCP SDK、长描述目录与窄 SK 回归均已覆盖;最新 `pnpm verify` 为 package **1276** + provision **1** = **1277 passed / 7 skipped**。
- 2026-08-11:**E2E-D 本地 final-image 一键栈证据已闭环**——`pnpm --filter @tool-bridge/server --prod deploy --legacy /out` 会留下指向 build workspace的 Dashboard symlink,该目标在 final stage不存在;production Dockerfile现将fresh-built `dist` 显式复制为 `/app/dashboard`,并设 `TB_UI_DIR=/app/dashboard`,由非root `node` 用户直接托管。final仍是单个 `node:22-bookworm-slim`进程、`CMD ["node","/app/dist/main.js"]`、单端口与仅 `/data` volume。Compose smoke除 gateway→plugin-feishu→mock upstream三跳外,还检查 `/ui/` 与SPA deep link `/ui/manage/registry` 的200 HTML/title。真实浏览器直接消费该 Compose final image,desktop/mobile共九路由无横向overflow;树请求边界为root=1/local=1/remote=3/filter=8,ARIA键盘与移动Escape焦点恢复通过,18次导航 `console warning=0/error=0`, `pageerror=0`。既有全量证据仍为 package **1273** + provision **1** = **1274 passed / 7 skipped**。**这只闭合本地E2E-D;浏览器流程尚未固化为仓库内Playwright spec,也不替代真实Cloudflare/D1/飞书/DO等其余生产PENDING。**
- 2026-08-11:**E2E-C 双宿主与三消费面已闭环**——shared contract、Miniflare D1、Node TCP/SQLite、真实 CLI 全 cursor 与本地 Dashboard 证据先行;Round 32 又在共享开发真实 D1 上以临时 visible/hidden fixture 对拍 `日程` / `create document`,admin 2→narrow 1,线上 Dashboard DOM 同样只见 allowed path。临时 SK、registry fixture 与 D1 行均已清理。
- 2026-08-11:**E2E-A 的确定性与在线证据已闭环**——gateway secretRef/device、Node bootstrap/DeviceHub barrier+mutation 与全包 38 tests 先行;Round 32 的共享开发 `smoke`、`verify-device` 标准路径和 `TB_VERIFY_HIBERNATION=1` 又验证真实 shell/fs、同 ID replacement、registerPaths allow/deny 及 155s 空闲后调用,临时设备与 SK 已清理。
- 2026-08-11:**localhost-only Docker Compose 三跳开发栈已本地落地并实跑闭环**——根 `docker-compose.yml` 启动 gateway production final image(仅 `127.0.0.1:8787` 发布)、内网真实 plugin-feishu Wrangler Worker与认证mock upstream;`smoke` profile通过 `docker compose run --rm` 同步验证Dashboard root/deep link、写SecretRef、注册plugin/v2、挂载export并调用出精确 `compose-roundtrip`,不是并列health假绿。`compose:up`启动三常驻服务,`compose:down`保留 `gateway-data`,`compose:reset`才删卷。证据覆盖fresh volume、同卷重复smoke、gateway restart、全新错误app id→upstream 401/gateway 503/进程非零后恢复、`HostIp=127.0.0.1`、final CMD与teardown。固定Admin/plugin/app/TAT fixture **只允许隔离localhost开发**,不得复制到生产。**mock route独立单测、smoke逐请求AbortSignal、并行worktree image tag与长期Playwright spec仍是增强项;本地证据不代表生产凭据、真实飞书、跨主机网络或部署。**
- 2026-08-11:**Dashboard Registry/Plugins/SK 系统表单拆分与管理面契约对拍已在本地完成**——wire payload 收敛到无 React/DOM 依赖的纯 builder;Registry 六 kind 字段与 `MountDialog`、Plugin manifest 字段与注册/编辑 dialog、SK 字段拆入 `pages/system/forms/`,三者共用 `FormSection`,route page 保留查询/mutation 编排及尚属本页的展示或 dialog。`NodePage` 与 Registry route 都直接依赖独立 `MountDialog`,不再从 route page re-export。对拍同时补齐 HTTP auth/ToolDef 校验、custom scheme 空前缀 fail closed、SK owner/每条 scope 半填 fail closed、registerPaths 按行解析以保留合法逗号,以及 plugin/v2 只抓 `~describe`、bearer 字段为 `secretRef`。Dashboard 已有 Node Vitest **10 passed**,并纳入根 `test:unit`;gateway fresh-source `ui.integration` **15/15**。真实浏览器核对 desktop/mobile 无横向溢出且 console 0。**Node 单测只证明 builder wire shape;dialog 生命周期、kind 草稿保留、Plugin 分页与一次性 token/secret 交互仍未被组件测试覆盖。**
- 2026-08-11:**root 全局工具搜索的自动同步、加权检索、权限后分页与 CLI/Dashboard/MCP 消费面已部署到共享开发环境**——StateStore/registry/tool cache 是 canonical,SearchIndex 是可重建派生状态;D1/SQLite v3 lightweight schema保留检索字段与完整 ToolSpec digest,长词 trigram FTS、短词 escaped LIKE、混合 AND。协议支持 limit/cursor(default 50/max 200),权限/virtualize 后分页与 AES-GCM cursor,并有 500 paths/20 KiB、raw work 400、完整响应 4 MiB、D1 Free 50 queries 等边界。当前仅 keyword;semantic 与外部 HTBP Draft 同步仍 PENDING。
- 2026-08-11:**工具搜索 D1 已真实 provision 并部署到共享开发环境**——`TB_SEARCH` → `tb-search` UUID=`f788b779-ec1c-4fba-ac1f-b780fab990fc`;首次 create 后配置回填,二次运行 KV/R2/D1 全部 skip。fake wrangler 幂等回归仍纳入 `test:unit`,真实 D1 则由 Round 32 双 query/admin-narrow smoke 验收。
- **初步实现阶段已完成**(2026-07-07 "破壳"):SK 鉴权与作用域、HTBP 核心树与内容协商、Tool 层(mcp/http/remote 联邦 + 虚拟化)、Context 层(r2/s3 四动词 + Search + `$ref` 大对象)、设备反向注册(DO WebSocket hibernation)、SDK、Plugin 系统、Dashboard 均已落地并经过在线开发环境验证。
- 2026-08-11:**安全报告部署 blocker 已闭环并在共享开发环境复验**——OAuth callback/CSP/安全头、Workers与Node bootstrap fail closed、Secret Reference 使用授权、canonical origin、RefToken用途域、DO/Node invoke scope/registerPaths与active-generation guard均已有测试;在线又完成 smoke、同 ID replacement 与155s hibernation。剩余风险是 KV最终一致、Dashboard localStorage SK、分布式 rate limit及未来正式 release 的 clean-main复验,不再是本轮部署 blocker。详见 [../memory/reflections/2026-07-24-security-report-remediation.md](../memory/reflections/2026-07-24-security-report-remediation.md)。
- 2026-07-22:**CLI 参数契约审查与修复已在本地完成,0.8.0 发布物已准备**——①SK `expiresAt` 在 CLI 与 core Write/Update 双层只接受带时区 ISO timestamp并规范为 UTC,认证读取对历史非法值 fail closed;②`--json/--base-url/--sk/--timeout` 可放根/命令组/叶子位置,Commander 解析错误在 `--json` 下也结构化且只认解析后的 option value/source(`--` 后同名文本不误触发),组级 help 展示 Global Options;`server add` 远端地址改为 `--remote-url`(`--base-url` 恒指当前网关,help 与缺参错误均含迁移提示);③status/login/tool auth 一次性 HTTP 接入统一 timeout,长驻 connect 明确拒绝不适用的 `--timeout`;④mcp/http/tool、r2/s3/plugin 等条件 flag 与多输入源本地拒绝冲突,挂载省略描述时派生非空值,tree 显式 depth 严格 1..8,help 同步依赖/互斥/范围/默认值与分页 fallback;⑤返回 Page 的 SK/Secret/Plugin/Context/Skill/Server/Device list/search 统一 `--limit 1..200`/`--cursor` 且过滤列表不丢 cursor;⑥补齐 `ctx rm`、SK get/update/enable/disable、tool/context Plugin Provider 挂载。`pnpm verify` 全绿(见常用命令),CLI 21 文件 **233 passed**;`packages/cli/package.json` manifest 已 bump 到 **0.8.0**,CLI build、实际入口 `--version`(回报 0.8.0)与 `npm pack --dry-run --json` 均通过。**尚未打 `cli-v0.8.0` tag、尚未发布/部署**;npm registry `latest` 仍是 **0.7.0**,发布时须明确 `server add --base-url` → `--remote-url` 迁移。
- 2026-07-07:修复"挂载 MCP 过一段时间失效"生产故障(不合规上游对过期会话回 200+空列表,见 [../guides/mcp-upstream-pitfalls.md](../guides/mcp-upstream-pitfalls.md))。2026-07-08 同故障复发:初版防御的重试回读 KV 被边缘读缓存击穿(拿回刚删的旧会话),已改为重试强制完整重握手(`forceFresh`,PR #8)并补钉死用例,**已部署上线**且经塞伪 session 复现验证自愈(工具列表恢复、KV 回填新会话)。
- 2026-07-08:`~help` 可读性重构:新增 `Accept: text/markdown` 可读表现(renderHelpMarkdown)、`hint` 行/字段(下一步指引)、索引形态 h 一句话摘要(此前上游 mcp 整篇多行 description 原样撑爆索引并破坏 DSL 行结构)、DSL 多行值安全化(node 行折叠单行、多行 h 续行缩进)、`tb help --md`;契约见 [../reference/protocol-contract.md](../reference/protocol-contract.md)。**已部署上线**(smoke 通过;生产实测根/mcp 索引/单工具三级 markdown 与索引一句话化均生效)。
- **Docker/Node 宿主部署路径已落地**(2026-07-08):`@tool-bridge/server` 包(SQLite + FS + ws DeviceHub)+ 根 Dockerfile + GHCR/npm 双发布 workflow;本机与 Docker 验收全过(smoke/verify-device/verify-plugin/重启持久)。见 [../guides/docker-host.md](../guides/docker-host.md)。
- 2026-07-08:**Path 补充说明 + Agent 反馈能力**落地(本轮)——①builtin `system/annotation`(七模块):admin 对任意 path(含工具子路径、根=全树公告)写补充说明,网关 `enrichHelp` 注入该 path `~help`(DSL `note` 行 / JSON `note` / Markdown Notes 节);②**`~feedback` 新保留段**(per-path 一级协议能力,非 builtin):`GET /<path>/~feedback`(列表,?hidden=1)/`GET .../<id>`(详情)/`POST`(提交 {title≤80,detail≤500},call scope)/`POST .../<id>`(投票 up|down|clear,每 owner 一票可改票)/`DELETE .../<id>`(admin);净分>-3 前 5 条注入 `~help` feedback 块(带 use 指引行,未知行忽略通道,DSL↔JSON 等价);权限判定落目标 path(窄 scope SK 天然可用);存储 `annotation:<path>` 单条 + `feedback:<path>` 单 key 数组(KV last-write-wins 接受并发窗口);CLI `tb note`/`tb feedback` + Dashboard NodePage(NoteCard + 反馈 tab)三入口对等;根 `~help` hint 与 `tb --help` 尾部文案双向引导 feedback 习惯(用前查经验、踩坑后回馈)。契约见 [../reference/protocol-contract.md](../reference/protocol-contract.md)。**已部署上线**(首次随 Worker version 43 生效;2026-07-10 生产只读复验 `GET /system/annotation/~help` 与节点 `GET /~feedback` 均为 200;此前本机 Node 宿主全链路 E2E 与 Dashboard 实操均已通过)。
- 2026-07-16:**skillhub 新 kind 落地**(本轮,与 context/device 并列的一等内容型 kind:Agent Skill 仓库)——每个 skill = 一个目录 `<id>/SKILL.md`(Claude 约定的 frontmatter name/description)+ 若干 UTF-8 文本文件;发布到平台自带 R2(provider r2,**无需外部凭证**;s3 可 opt-in)。存储引擎完全复用 context 的 ObjectStore/ObjectContextProvider(etag 版本、`$ref`/`~ref` 大对象、按 `skills/<path>` 前缀隔离、ttl/readOnly);唯一净新逻辑是 core `skillhub/`(frontmatter 最小解析器 + `createSkillhubProvider` 的"以 skill 为单位"分组)。数据面动词:`List`/`Get`(含 `{id,file}` 取单文件)/`Search`(read)+ `Publish`(整体替换,校验含 SKILL.md 且 name/description 必填)/`Remove`(write);服务端解析 frontmatter → `List` 目录(id/name/description/version)、`Search` 覆盖 name/description(渐进式发现)。三入口对等:CLI `tb skill mount/publish/ls/get --out/search/rm/unmount`(`get --out` 把 skill 落到本地 `.claude/skills/<id>/` 直接可用)+ Dashboard(KindBadge/NodePage SkillBrowser/RegistryPage 挂载表单全集成)+ API。核心接线与既有 `tool` kind 同路径(`types.ts` NodeKind/NODE_KINDS/NodeConfig + 网关 handleInvoke/helpModelFor/handleDescribe/assertSkillhubConfig/`~ref` 泛化);`~skill` 保留段(节点使用指南)与 `skillhub` kind 语义正交,不冲突。新增 gateway `skillhub.integration.test.ts`(9 例,真实 workerd + 本地 R2)+ cli `skillhub.test.ts`(12 例);`~skill` 保留段本地仍 501 占位未变。代码已包含在 Round 32 gateway 部署中,但本轮未创建或操作共享开发环境的 skillhub 挂载。
- bootstrap 期过程文档已归档 `archive/`;知识真源 = 代码 + llmdoc(见 [project-brief.md](project-brief.md))。
- 2026-07-08:**CLI 与 API 描述面向 Agent 重整**(本轮)——①builtin 七模块(sk/secret/registry/status/plugin/federation/annotation)每 cmd 补 `h` 一句话说明,inputSchema 属性全量补 `description`(owner ref 格式、Scope 结构 items schema、opts 分页语义等;新增 `util.ts` `LIST_OPTS_SCHEMA` 共用分页 schema,sk/registry 的 write/update.patch 共享字段 schema 常量);②context 六动词 `h` 与 device shell(`describeAllow`/exec/`SHELL_DESCRIPTION`)文案由中文改英文(~help 消费方是任意 Agent,统一英文口径;shellExecutor permission_denied 消息同步,4 处钉住测试已同步);③CLI:顶层 description 加起步指引(login→ls/help→call),call/sk create/tool mount/ctx/connect 补 `addHelpText` Examples,whoami 描述去噪。**已发布/部署**(CLI 由 npm 0.6.1 承载,API 描述首次随 Worker version 43 生效)。
- 2026-07-08:**CLI markdown 终端富渲染**(本轮)——`tb help` 与 `tb call`(人类模式)的 markdown 输出按 stdout 形态分流:TTY → marked-terminal 渲染 ANSI 富文本(标题着色、表格盒线、代码高亮),管道/重定向/Agent 捕获或 `NO_COLOR` → 裸 markdown 原样(对机器最优形态);`tb help --md` 语义从"无操作别名"变为**强制裸 markdown**(TTY 下也不渲染,便于复制/落文件)。落点 `cli/src/markdown.ts`(`printMarkdown`/`shouldRenderAnsi`,渲染失败降级裸输出);新依赖 marked@15(marked-terminal peer 上限 <16,勿升 16+)+ marked-terminal@7(@types/marked-terminal 的返回类型标错需断言桥接)。**已发布**(`@tool-bridge/cli` 0.6.1;verify 全绿,TTY/管道双形态已实测)。
- 2026-07-08:**直连工具调用**上线(PR #9)——`POST /<node>/<tool>` body 即 arguments 本体,`~help` 宣告直连路径(CmdSpec `flatBody`);信封入口 `POST /<node>` + `{tool,arguments}` 保留。CLI(`tb call <node>/<tool>`,--tool 变 optional)与 Dashboard(CmdPanel 按 cmd.path 判别、CliHint 同步)同轮对齐。已部署上线并经生产直连调用实测。
- 2026-07-08:**mcp 托管 OAuth 上线并经真实上游验证**——`config.auth:'oauth'` 的 mcp 挂载由网关全托管授权码流程(SDK auth() 编排 discovery+DCR+PKCE;`POST /<path>/~authorize` 发起、`GET /~oauth/callback` 回调,state 为 AES-GCM 加密自包含载荷零 KV 存储;token/client/discovery 落 `mcpoauth:*`,SDK 自动刷新;CLI `tb tool mount --auth oauth` + `tb tool auth`;Dashboard 挂载面认证三选 + 列表钥匙按钮)。**严格上游降级通道**:Bytebase 等上游 DCR 硬编码 redirect 白名单只放行 loopback/知名客户端(不可配置),`~authorize` 可带 body `{redirectUri}`(仅 loopback)+ `tb tool auth --local`(本机临时端口收回跳,code 转交网关兑换)。已部署并经生产 Bytebase 实测全链路(--local 授权 → 工具列表 → 真实调用)。已知限制:refresh token rotation 上游 + 多 isolate 并发刷新可能互相作废(失败回「重新授权」指引可自救)。
- 2026-07-08:**内容协商默认翻转为 markdown**——所有端点默认(无 Accept/`*/*`/未知类型)返回 `text/markdown`,声明 `Accept: application/json` 才回 JSON;紧凑 Help DSL 保留但降为显式通道(`Accept: text/plain`;`~tree` 同理,markdown 表现为 code fence 包缩进树)。归类唯一入口 core `htbp/negotiate.ts`。CLI:`tb help` 默认 markdown、新增 `--dsl`、`--md` 变默认别名;Dashboard:节点页「~help 原文」DSL tab 换成「~help 文档」markdown tab(react-markdown 渲染)。内部消费不受影响(plugin 契约抓取与 Dashboard 表单显式 Accept json;remote 透传原样转发 Accept)。契约见 [../reference/protocol-contract.md](../reference/protocol-contract.md)。**已部署上线**(2026-07-08:smoke 通过含新双表现断言;生产 curl 实测默认 markdown / text/plain DSL / json / ~tree fence 四形态均生效)。
- 2026-07-08:remote 联邦 host 白名单从"部署期 env-only"扩为"env 基线 ∪ 运行时可增删"——新增 builtin `system/federation`(admin scope)+ `RemoteAllowlistStore`,三入口对等(`tb federation ls|add|rm` + Dashboard「联邦白名单」页 + API);env 基线条目只读不可删。**已部署上线**(evilstar 账户)。
- 2026-07-08:Dashboard 树发现性能修复(远端联邦树慢)——根因是根 `~tree` 聚合 remote 子树时 `getChildren` 对**每个**远端节点单独 fetch 上游(N+1,首屏 21s;上游自身仅 1.8s)。修复两处:①前端 TreeNav 改**懒加载**(首屏 depth=1 不碰远端,展开某节点按需拉;remote 节点/其后代走纯透传 depth=3,本地目录 depth=1 免 N+1;NodePage/TreeNav 对纯透传返回的远端 path 做 localize 补挂载前缀);②core `buildTree` 加 `opaqueKinds`——深度边界(`depthLeft<=0`)对 remote 节点免 fetch 直接标 truncated(gateway 传 `{'remote'}`),消除边界探测的远端往返。实测首屏 21s→<1s(热)、展开 remote 目录 7s→<1s。**已部署上线**。
- 2026-08-12:**npm 版本对齐轮完成,六包 registry latest 与 main 全部对齐;app/server 的 CI 发布通路首次实证**——registry 实测 **cli 0.8.0 / sdk 0.5.0 / gateway 0.5.0 / dashboard 0.7.0 / app 0.1.1 / server 0.1.1**,与 `packages/*/package.json` 逐包核对一致。app/server 两包**第一次尝试失败**,修复后删 tag 重打成功(run `31584210300` / `31584336836`,同 tag 的 `publish-docker` `31584336835` 也成功);两者 `dist.attestations` 均为 SLSA v1 provenance(**手动发布签不出 provenance,这是 CI 通路的实证**,app 919,628 B / server 2,433,797 B)。**首次失败的两个 run 都挂在发布前的构建/冒烟步骤,与 Trusted Publishing/OIDC 无关**:①`publish-app` `31579464648` 挂在 `pnpm --filter @tool-bridge/app build`——`packages/app/tsup.config.ts` 是六包里唯一写 `dts: { resolve: true }` 的,`true` 内联所有外部类型,dts rollup 读 `zod/v4/index.d.cts` 时无法解析其指向 `index.cjs` 的 default 导出直接报错(本地 100% 复现);②`publish-server` `31579525102` 挂在 `Smoke dist`(exit 7)——安全修复后 Node 宿主首次引导缺 `TB_BOOTSTRAP_ADMIN_SK` 即拒绝启动,冒烟步骤裸起进程没跟上该语义。**根因共性:`pnpm verify` 不跑 build,发布 workflow 是 build 的第一道闸门**,故这类断裂只在打 tag 后暴露、返工要删 tag 重打;已在 `.github/workflows/ci.yml` 补 `pnpm turbo run build`(七包约 6s)当 PR 闸门。发布后消费者验证:从 **registry** 装 app@0.1.1 + server@0.1.1,server 的 regular dependency `@tool-bridge/dashboard` 正确解析并实装 0.7.0,裸起 fail closed 生效、给 SK 后 `/healthz` 返回 `{"healthy":true,"version":"0.1.1"}`;app tarball 另在仓库外隔离目录以 `skipLibCheck: false` 跑消费者 tsc 通过。[PR #42](https://github.com/TokenRollAI/tool-bridge/pull/42) 已合入。
- **npm 已发布**(四包均经 CI Trusted Publishing;core 为 private 不发布):`@tool-bridge/cli` 0.7.0(2026-07-10,Agent 体验修复:positional args / retryable / --timeout / feedback hint;tag `cli-v0.7.0`,run `29092102298` 成功且 npm `latest=0.7.0`)、`@tool-bridge/sdk` 0.4.0、`@tool-bridge/gateway` 0.4.0、`@tool-bridge/dashboard` 0.6.0(2026-07-10,tree-first 整站大修;tag `dashboard-v0.6.0`,Trusted Publishing run `29068385868` 成功且 npm `latest=0.6.0`)。gateway/dashboard 的 Trusted Publisher 已配置生效。**`@tool-bridge/app` 0.1.0 已于 2026-08-12 手动首发并配好 Trusted Publisher**(`npm view` 实测 `latest=0.1.0`,入口 main/types/exports 全指 dist 说明 `pnpm pack` 覆盖生效,4 文件 / 482.4 KB,`repository.url` 正确;手动发布无 provenance 故 `dist.attestations` 为 null,符合预期)。`@tool-bridge/server` 0.1.0 同日手动首发(8 文件 / 2.38 MB)。**六包首发全部完成,且六包的 CI 发布通路均已实证**——app/server 的 0.1.0 是手动首发(不签 provenance,`dist.attestations` 为 null,**首发成功不代表 CI 能发**),2026-08-12 的 0.1.1 才是两者第一次经 CI Trusted Publishing 发布(首次尝试即因上条的两处断裂失败,修复后成功,provenance 为 SLSA v1)。**坑:发布后立刻 `npm view` 可能仍报 E404**——registry CDN 有传播延迟,我在 server 创建后 91 秒查到的仍是 404,据此误报「未发布」;判定发布结果要么等一两分钟复查,要么直接看 `npm view <pkg> time.created`。发布流程见 [../guides/npm-publish.md](../guides/npm-publish.md)。**坑:一次推多个 tag(`git push origin tag1 tag2 …`)不触发 tag workflows,须逐个 push**(2026-07-08 实测:四 tag 同推零触发,删除后逐个重推全部触发;同日逐个推四 tag 复验全部正常触发)。
- 2026-07-08:**mcp 上游自定义请求头**落地——mcp NodeConfig 新增 `authHeader`/`authScheme`(与 http 同语义)与静态明文 `headers`(凭证头覆盖同名),动机是接入飞书官方 MCP(`X-Lark-MCP-UAT/TAT` 原样注入 + 必带 `X-Lark-MCP-Allowed-Tools`,见 [../guides/mcp-upstream-pitfalls.md](../guides/mcp-upstream-pitfalls.md));CLI(`--auth-header/--auth-scheme` 变 mcp/http 共用、`--header` 仅 mcp)与 Dashboard 挂载表单同轮对等。**已部署上线**(2026-07-08 随 0.3.0 内容协商部署一并上线);飞书未走直挂形态(生产采用 plugin-feishu,见下条),直挂静态凭证通道保留未实际使用。该扩展保留不回滚:直挂静态凭证的 MCP 上游仍是合法路径。
- 2026-07-08:**plugin-feishu 新包**(commit ae8ab02)——飞书官方远程 MCP 的 tool-provider/v1 plugin(CF Worker,自部署进用户 CF 账户),动机:TAT 约 2h 过期,plugin 内自动换发 + 上游 401 强制重换发自愈,免人工 `tb secret set` 续期;凭证会过期的上游推荐此形态,直挂 kind:mcp + 静态 TAT 适合一次性验证。**已部署已注册已挂载**(2026-07-08 实测)——Worker `tb-plugin-feishu` @ https://tb-plugin-feishu.shuaiqijianhao.workers.dev(DJJ 账户);生产网关 `tb plugin register`(id=feishu,platform-token)+ 挂载树节点 `feishu`(kind:tool,authRef=feishu-app);`tb help feishu` 经完整链路(网关→plugin→TAT 换发→飞书 MCP)列出 8 工具;**业务调用已生产复验通过**(2026-07-08,用户开通 docx 应用权限后):create-doc 建档、fetch-doc 读回、update-doc append 追加(注意 `mode` 参数必填)全链路成功;list-docs 仍差 drive:drive 权限(99991672,用户按需开通)。**注意**:TAT 应用身份创建的文档归属应用,用户账号默认不可见,须 create-doc 传 `folder_token`/`wiki_node` 落到用户可见目录(应用需先被加为该目录协作者)或经 add-comments @用户 分享。
- 2026-07-08:**plugin 上游凭证边界重构**(commit 8af6914,已生产迁移 + 终验)——plugin 上游凭证(飞书 app_id/app_secret)不再由 plugin Worker secret 自持,改存平台 SecretStore,kind:'tool' config 新增 `authRef`,网关每次调用 resolve 后经新头 `X-TB-Upstream-Auth`(base64url)注入 plugin(resolve 失败 → unavailable 快速失败);动机:公网 endpoint 的 plugin 自持凭证时 PLUGIN_TOKEN 泄漏即凭证可用、换凭证须重部署,现在 plugin 无凭证即空壳,轮换只需 `tb secret set`。plugin-feishu 同步:TAT 缓存与 MCP 会话按 app_id 键控(多凭证挂载不串号,有测试钉住)。生产迁移完成:网关与 plugin 重部署、plugin 三 secret 删至仅剩 PLUGIN_TOKEN、挂载节点 `feishu` 配 `authRef:"feishu-app"`、`tb secret set feishu-app`(JSON {app_id,app_secret})后 create-doc 经新路径写团队 wiki 终验通过。
- 2026-07-10:**Dashboard 0.6.0 整站大修已发布并在生产生效**——信息架构重整为 `ActivityRail` + 可折叠 `ExplorerPanel` + 全宽 Workspace,移动端资源抽屉关闭后恢复焦点;TreeNav 保持 root depth=1、本地截断懒取 depth=1、remote 及后代纯透传 depth=3、仅非空过滤取 root depth=8,并以受控展开/可见顺序/焦点状态实现 ARIA tree roving focus。`NodePage` 继续作为通用协议回退,`CommandWorkspace` 只挂当前 `CmdPanel`,Context 使用桌面 master-detail/移动 dialog 且保持正文/version/`$ref` 原子基线;六个系统页对齐 builtin/CLI 的已知工作流、cursor 边界与危险操作 Promise 结算,历史/session/lazy route 安全边界延续 0.5.0。真实浏览器终验覆盖桌面/移动全部路由、树请求边界与键盘导航,无 document 横向溢出且 console 无 error/warning;SK 一次性值、Secret upsert 与 Registry replace 警告均已实操。`pnpm verify` 全仓 **1005 passed / 7 skipped**,退出码 0。发布证据:commit `e363e01`,tag `dashboard-v0.6.0`,Actions run `29068385868` 成功,npm `@tool-bridge/dashboard` 的 `latest=0.6.0`;生产 `/ui/` 首次由 Worker version 44(`eb4e8daf-7b5d-44ff-a91b-5b8074348854`)承载,HTML SHA-256 为 `2420378ce98842928fdad91c5ad87ae2f3956bb83c2853c929f1abd7d550a264`,生产 smoke 通过。后续纯文档提交可能生成内容等价的新 Worker version,故当前部署序号应以 `wrangler deployments list` 为准;Gateway `/healthz` 版本仍为 0.4.0。
- 2026-07-10:**CLI Agent 体验修复**(本轮,源自真实 agent 使用反馈:参数试错/超时不可控/retryable 丢失/feedback 机制零使用)——①`tb call` 第二 positional 收裸 JSON arguments(`tb call <path> '{...}'`;与 `--args`/`--args-file` 三源互斥,误写 `--json '{...}'` 形态自然工作,严格解析不破坏:第三个 positional 仍硬失败);②CLI 错误呈现 TBError `retryable`(此前在 `toCliError` 被丢弃):人类模式加 `(retryable — try again)` 尾注、`--json` 错误对象带 `retryable` 字段;③全局 `--timeout <seconds>`(默认 120s,`AbortSignal.timeout` 实现,超时报 retryable 错误并提示加大 --timeout);④**失败现场 feedback 引导**:`tb call` 失败(unavailable/internal/invalid_argument/rate_limited)时尽力拉取该 path `~feedback`(限时 5s、失败静默),有条目 stderr 列 top 3 + `tb feedback get` 下钻命令(--json 带结构化 `feedback`),无条目引导 `tb feedback submit`——把已建成但闲置的经验闭环推到 agent 注意力所在的报错现场;⑤core 上游错误 message 英文化(upstream unavailable/returned HTTP N,统一英文口径)。落点:cli `http.ts`(CliError+retryable/hint/feedback、apiFetch 超时)/ `args.ts`(--timeout)/ `output.ts`(reportError)/ `commands/call.ts`(positional+attachFeedbackHint)、core `tool/upstreamError.ts`。新增 `test/callUx.test.ts`(16 例),CLI 178 测试全绿,verify 全仓通过;生产实测 positional 双形态/超时/invalid_argument 触发 submit 引导。**已发布**(`@tool-bridge/cli` 0.7.0,PR #22 merge 后 tag `cli-v0.7.0` 触发 run `29092102298` 成功,npm `latest=0.7.0`)。

## 已部署资源(DJJ 账户)

| 资源 | 名称/地址 | 备注 |
|---|---|---|
| Worker | `tb-gateway` @ https://tool-bridge.pdjjq.org | custom domain(zone pdjjq.org);`wrangler.jsonc` 已写死 `account_id`;DO `DeviceSession` 绑定 `TB_DEVICE`(migration v1,sqlite);Dashboard 经 Static Assets 挂 `/ui`(`run_worker_first: true`);Dashboard 0.6.0 的上线证据为 Worker version 44(`eb4e8daf-7b5d-44ff-a91b-5b8074348854`),后续内容等价部署不改变该发布事实;运行时 `/healthz` 版本属 Gateway 0.4.0,Static Assets 中 Dashboard 为 0.6.0(两者版本归属不可混用) |
| Worker secrets | `TB_BOOTSTRAP_ADMIN_SK` / `TB_SECRET_ENCRYPTION_KEY` | 已 `wrangler secret put`;前者是 Admin SK 明文(引导时 sha256 入库) |
| KV | `tb-kv`(id `d18c93de33cf4ba2b1fbf7d26fd742f1`) | 绑定名 `TB_KV`;id 已回填 wrangler.jsonc |
| R2 | `tb-r2` | 绑定名 `TB_R2` |
| Worker | `tb-plugin-feishu` @ https://tb-plugin-feishu.shuaiqijianhao.workers.dev | 飞书 MCP tool-provider plugin,secrets 仅 `PLUGIN_TOKEN`(飞书凭证存平台 SecretStore `feishu-app`,经挂载 authRef 注入),生产网关注册 id=feishu、挂载路径 `feishu`(authRef=feishu-app) |

## 代码现状(pnpm monorepo:核心包与内置插件均在 `packages/*`(插件为 `packages/plugins/src/<name>/` 源码文件夹);共享依赖版本唯一真源是 `pnpm-workspace.yaml` 的 catalog,包内写 `catalog:`;插件 tsconfig 统一 extends 根 `tsconfig.plugin.json`。测试数为 2026-08-14 实跑)

- `packages/core` — 纯逻辑内核(唯一运行时依赖 zod),**827 个单测**,模块族:
  - `auth/`(scope 判定 / authorizer / 注册路径规则 / sk 签发与哈希)、`tree/`(path 规则 / NodeRegistryStore / visibility 裁剪)、`htbp/`(helpDsl / helpMarkdown / summary / HelpModel / negotiate / tree 构建)、`secret/`(AES-256-GCM 只写不读)
  - `builtin/`(**八模块**:sk / secret / registry / status / plugin / **catalog** / federation / annotation 的 cmd 表 + dispatch;`catalog.ts` 是内置集成目录的只读浏览面,三 cmd 全 scope=read,`system/plugin` 同时收窄为 external-only)、`annotation/`(AnnotationStore)、`feedback/`(FeedbackStore:排序/阈值/top-5 选条真源)
  - `tool/`(HttpToolDef 拼装、虚拟化、mcp schema→HelpModel、remote 路径/白名单/Via、上游错误归一、**RemoteAllowlistStore** 运行时白名单存储);`search/`(lightweight candidate `SearchIndex` + snapshot `MutableSearchIndex`,FTS/LIKE hybrid、snapshot/digest/budget、AES-GCM cursor 与分页 contract;完整 ToolSpec 由宿主从 canonical state 水合)
  - `context/`(四动词 objectProvider / objectStore 接口 / path 穿越防护 / ttl)
  - `device/`(帧编解码 / 会话状态机 / 设备侧 client / shell 白名单 / helpModel)
  - `plugin/`(manifest 校验 / envelope 编解码 / RequestDedupe / 契约校验;**`catalog.ts`** = 内置目录类型 + `resolveBuiltinExport`/`resolveExternalExport`/`resolveIntegration` 唯一分发口 + canonical JSON 与两级 digest,只吃 `ReadOnlyStore` 故**结构上写不了库**)
  - `node/`(`./node` 子导出:FsObjectStore realpath 防逃逸 + shellExecutor 有界缓冲)
  - 顶层 `errors.ts`(TBError)/ `store.ts`(StateStore 接口 + 内存实现)/ `types.ts`。
- `packages/app` — **宿主中立应用层**(manifest 0.4.0 待发,registry latest 0.1.1;tsup `platform: 'neutral'` bundle core;tsconfig `types: []` 让编译器而非纪律保证中立):`tbApp.ts`(144 行,**只做装配**:路由挂载顺序 = 安全语义)+ 按关注点拆开的协议行为(`deps`/`responses`/`paths`/`federation`/`deviceNodes`/`toolNodes`/`contextNodes`/`helpModel` 与 `routes/*` 九个路由模块,最大 482 行)、`bootstrap.ts`(引导八个 builtin;`BuiltinAssemblyOpts.pluginCatalog` → `buildDeps` 注入 catalog 模块)、`deviceHello.ts`、`mcpServer.ts`(向无状态 `/~mcp` 投影稳定 `tb_search`/`tb_help`/`tb_list_nodes`)、`oauth.ts`、`refToken.ts`、`search/synchronizer.ts`(registry/tool cache/device/feedback 自动投影、批量 canonical ToolSpec 读取与 LKG 恢复)、`providers/`(mcp/http/remote/toolCache/s3Object/s3Sign/pluginClient/pluginTool/pluginContext)。注入面新增 `TbAppDeps.pluginCatalog`(与 `pluginBindings` 同源装配);`toolNodes.ts` 的 `requirePluginExport` 现委托 core `resolveIntegration`,`autoRegisterBinding` 已删。`test/` 是它自己的 Node 套件(15 文件 **163 例**,直打 `createTbApp`;`harness.ts` + `memorySearchIndex.ts`;`pluginBinding.integration.test.ts` 钉住免注册挂载、零写库与「只给 bindings 不给 catalog」的装配失配),**test/ 也在 `types: []` 约束内**。
- `packages/gateway` — Workers 宿主适配器(可发布 Worker library:tsup 单文件 ESM bundle core+app,`src/index.ts` export `createApp`/`type Env`/DO;exports 仅 `.`,发布形态 publishConfig 覆盖指 dist),约 1200 行:`app.ts` 做 Env→deps 适配并在可选 `TB_SEARCH: D1Database` 存在时注入 `D1SearchIndex`;另有 `deviceSession.ts`(DO/WS hibernation)、`kvStateStore.ts`、`providers/r2Object.ts`、`search/d1SearchIndex.ts`(**115 行,只剩驱动**:batch/prepare 接线 + JSON1 攒块 + 50 查询预算;v3 逻辑在 core `search/sqlSearchIndex.ts`)。**其 197 例 workerd 集成测试同时是 app 中立层的事实验证面**:shared/SELF/HTTP、官方 MCP SDK、并发 capacity、自动同步、长描述目录、反馈权重、分页与预算测试已覆盖。`wrangler.jsonc` 已绑定真实 `tb-search` D1 UUID `f788b779-ec1c-4fba-ac1f-b780fab990fc`;v3 已随 gateway version `2221abb7-bd12-4c00-8bc2-8918d5bae9b4` 部署并完成在线对拍。
- `packages/cli` — commander 框架(manifest **0.16.0 待发**;registry `latest=0.13.0`;严格解析:未知 flag/子命令报错,防权限误配),**292 个单测(25 files)**(含 Cloudflare 新旧实例/secret/profile/fail-closed 编排、动态叶子 strictParsing、root search wire/输出、callUx、argSemantics、helpContract 与 integration 安全回滚),**24 个顶层命令族**:init / status / login / whoami / use / sk / secret / federation / note / feedback / ls / tree / search / help / call / tool / server / ctx / skill / connect / device / mount / plugin / integration;除 `init cloudflare` 从源码 checkout 编排部署外均走公开 API,无专用管理端点;全局 `--json` / `--base-url` / `--sk` / `--timeout`;配置 `~/.config/tool-bridge/config.json`(XDG,多 profile);同一完整命令面可由 Bun 编译为本机单文件或根 `Dockerfile.cli` 的 Linux amd64/arm64 非 root 镜像。
- `packages/sdk` — 薄装配层(4 个源文件,manifest 0.8.0 待发),**19 个通过 + 1 个 opt-in skipped**;公开面 `createToolBridge(config)` → `{ fetch, registerTool, registerContext, connect }`,复用 core + app 的 createTbApp;`ToolBridgeConfig` 有 `pluginBindings` + `pluginCatalog` 两个装配口;再导出内存宿主实现(MemoryStateStore 等)。
- `packages/dashboard` — React 19 + Vite + Tailwind 4 + shadcn/ui + @rjsf + TanStack Query SPA(可发布纯静态产物包:只发 dist,依赖全在 devDependencies;manifest **0.14.0 待发**),Node 环境 Vitest **66 tests**(4 文件)覆盖 Registry/Plugin/SK builder、集成编译、无碰撞内部槽位及两个挂载 dialog 的凭证生命周期,已纳入根 `test:unit`。tree-first 工作台由 `AppShell` 组合 `ActivityRail`、可折叠 `ExplorerPanel` 与 Workspace,移动端资源抽屉保持关闭焦点恢复;`TreeNav` 按 root=1/local=1/remote=3/nonempty-filter=8 的查询边界懒取并 localize remote path,受控状态统一 expanded、可见顺序与 ARIA tree roving focus。独立 `/search` 当前消费 keyword root search,区分未提交/loading/401·403/404/空态并按 opaque cursor 续页;点击结果导航 NodePage并以 `?tool` 预选命令,TreePath 在 UI 导航与 HTBP API 请求前都逐段编码。
  `NodePage` 是未知节点的通用协议回退并直接复用独立 `MountDialog`,`CommandWorkspace` 只挂当前 `CmdPanel`,`CmdPanel` 的 mutation→invalidate 由独立 `mutateAsync` Promise 链结算;工具名含 `/` 时不冒充单个 URL segment,自动退回节点信封调用与 JSON 参数编辑。Context 浏览器为 master-detail/dialog,List/Search/cursor/metadata/`$ref` 与正文/version/`$ref` 原子基线同在。SK·Registry·Devices·Secrets·Plugins·Federation 六页对齐 builtin/CLI 的已知工作流、cursor 知识边界和危险/并发操作语义;其中内置 Registry 集成凭证由平台自动加密绑定且不展示 authRef,external/custom plugin 仍保留高级兼容输入。调用历史 v2、profile/BaseURL/revision 隔离、路由 lazy 与 `AppErrorBoundary` 安全边界继续保留。无专用后端(同源消费 HTBP);gateway Vitest 每次从当前 Dashboard source 重建后再跑 `ui.integration.test.ts`,真实浏览器已覆盖 desktop/mobile、搜索分页与结果预选,且 console 0、无横向溢出。`pnpm deploy:all` 先 build dashboard 再部署 gateway。
- `packages/plugins` — 内置插件目录(private,不发布,由 gateway/server 各自 bundle),**3980 个测试(106 files)**:`src/<name>/` 每插件一个源码文件夹(99 个 loader),`src/registry.generated.ts` 是装配表、`src/catalog.generated.ts` 是 descriptor 目录(**编译期常量**,99 个插件 / 35.9 KiB,含 per-entry digest 与 `BUILTIN_CATALOG_DIGEST`),两者各有 codegen 与闸门(`scripts/generateCatalog.ts` 求值 `~describe`;`test/runtime/catalogCodegen.test.ts` 逐条对拍)。tsconfig 分两份:`tsconfig.json` 管 `src`/`test`(宿主中立,无 Node 全局)、`tsconfig.node.json` 管 `scripts`(codegen 要 node:fs),typecheck 跑两个 project。
- `packages/plugins/src/feishu` — 飞书 tool-provider plugin(private,CF Worker),**9 个集成测试**(真实 workerd,mock 换发接口与 MCP 上游,默认离线):TAT 自动换发与缓存、契约面/envelope、401 强制重换发自愈、凭证头缺失/坏形状、多租户按 app_id 键控不串号;文件职责见 code-map。
- `packages/server` — Node/Docker 宿主胶水(可发布 bin `tool-bridge-server`,manifest 0.3.0 待发;**`main.ts` 装配全量内置目录**(`builtinPluginBindings` + `BUILTIN_CATALOG`),与 Workers `deployEntry.ts` 对等——此前 Node/Docker 官方镜像 catalog=0 而 Workers 99 个;`@tool-bridge/plugins` 进 devDep 且 tsup `noExternal`,tsconfig 因编译插件源码补 `lib: [...,"DOM","DOM.Iterable"]`):`sqliteStateStore.ts` 与 v3 `sqliteSearchIndex.ts`(**82 行,只剩驱动**:同步 API 包 Promise + transaction,无插入预算;v3 逻辑在 core `search/sqlSearchIndex.ts`)共用 `state.sqlite3` 文件但各持独立连接/独立表,提供 lightweight ToolSpec projection/feedback FTS/meta/digest snapshot、事务化容量与 cursor,由 `server.ts` 注入/关闭;旧 full-row 搜索表不迁移,首次 canonical audit重建。共享 adapter contract、同库异表、自动 registry sync、真实 HTTP root search 与 restart persistence 均有测试。首次引导缺 `TB_BOOTSTRAP_ADMIN_SK` 默认 fail closed且不监听/不打印明文;仅显式 `TB_ALLOW_INSECURE_BOOTSTRAP=true` 保留随机生成并打印一次的本地逃生阀。`DeviceHub.invoke` 在异步认证后复核 scope/registerPaths 与 active connection generation,barrier + mutation probe钉住同 ID 替换窗口;server 全包当前 **38 passed**。其余宿主面仍为 `config.ts` / `objects.ts` / `deviceHub.ts` / `assets.ts` / `server.ts` + `main.ts`;tsup bundle core+app(均 noExternal),better-sqlite3/ws/hono 等留 external;根 Dockerfile 产出单容器镜像。宿主中立 `runBootstrap` 与未传 `adminSk` 的 SDK 仍保留随机兼容行为,不得与 Node server 默认值混同。详见 [../guides/docker-host.md](../guides/docker-host.md)。
- `scripts/` — `gen-dev-vars.mjs` / `provision.mjs`(KV/R2/D1 幂等创建与 KV/D1 id 回填;`provision.test.mjs` fake wrangler 二次运行回归)/ `smoke.ts`(要求显式 base URL + SK;healthz + 无 SK 401 + 带 SK markdown/DSL 200,缺任一参数网络前 fail closed)/ `release-plan.mjs`(纯读:local vs registry latest、拓扑排序发布顺序、"发了会装不上"的阻塞项)/ `check-version-bumps.mjs`(PR 期提醒,退出码恒 0)+ 可重跑验收脚本:`verify-revocation.ts` / `verify-device.ts` / `verify-plugin.ts`(通用 context plugin/v2,不是飞书三动词)/ `verify-mcp.ts`(官方SDK双身份list+call)/ `verify-search.ts`(只读 exact-query admin/narrow CLI全分页对拍;不创建fixture)。
- CI(.github/workflows/):`ci.yml`(main push + 全部 PR 跑 `pnpm verify` + `pnpm turbo run build`,PR 另跑 `check-version-bumps.mjs` 当 warning)+ `release.yml`(**一轮多包发布的编排器**,2026-08-14 新增:`workflow_dispatch` 一次发完全部待发包 —— 算顺序 → 打 tag → `gh run watch` 串行等每个 publish 跑完;只编排不自己发,拒非 main,tag 已存在则跳过)+ `publish-cli.yml`(tag `cli-v*`)/ `publish-sdk.yml`(tag `sdk-v*`)/ `publish-app.yml`(tag `app-v*`)/ `publish-gateway.yml`(tag `gateway-v*`)/ `publish-dashboard.yml`(tag `dashboard-v*`)/ `publish-plugin-sdk.yml`(tag `plugin-sdk-v*`)/ `publish-server.yml` + `publish-docker.yml`(同 tag `server-v*`:npm 发布 + GHCR 镜像 `ghcr.io/tokenrollai/tool-bridge`),npm Trusted Publishing(OIDC,**信任按 workflow 文件名授予** —— 别把 publish 动作搬进 `release.yml`)。
- 工具链:lint 用 ESLint 9;测试 vitest 4 + @cloudflare/vitest-pool-workers 0.18(API 变更注意见 [../guides/workers-kv-pitfalls.md](../guides/workers-kv-pitfalls.md))。

## 常用命令

- `pnpm verify` — typecheck + lint + 单测 + 集成测试一把过(2026-08-14 逐包实跑:core 827 / plugins 3980 / cli 274 / app 163 / gateway 76(6 skipped)/ server 38 / sdk 19(1 skipped)/ plugin-sdk 33 / dashboard 41 = 包测试 **5451** + provision **2** = **5453 passed / 7 skipped**,退出码 0)。**`verify` 不跑 build**,故收尾还要 `pnpm turbo run build`(7 包)—— 那是发布 workflow 的第一道闸门。
- `pnpm test:provision` — fake wrangler 验证 KV/R2/D1 首次创建、id 回填与二次运行不重建;已纳入 `test:unit`。
- `pnpm release:plan` — 纯读的发布计划(local vs registry latest、拓扑排序顺序、阻塞项);真发走 `gh workflow run release.yml -f dry-run=false`,细节见 [../guides/npm-publish.md](../guides/npm-publish.md)。
- `pnpm --filter @tool-bridge/plugins generate:catalog [--check]` — 重新求值内置插件 `~describe` 生成 `src/catalog.generated.ts`(改了插件声明必跑;`--check` 只比对不写盘)。`generate:registry` 管装配表,`generate` 两个都跑。
- `pnpm deploy:all` — 幂等 provision(KV/R2/D1) + dashboard build + 部署 gateway;会操作真实 Cloudflare 资源。
- `pnpm --filter @tool-bridge/server start` — 本机起 Node 宿主(默认 :8787,数据落 ./data;env 面见 [../guides/docker-host.md](../guides/docker-host.md))。
- `docker build -t tool-bridge . && docker run -p 8787:8787 -v tbdata:/data …` — Docker 路径,验收命令全套见 [../guides/docker-host.md](../guides/docker-host.md)。
- `pnpm compose:up` / `compose:smoke` / `compose:down` / `compose:reset` — 本地三跳开发栈;分别启动常驻服务、同步 one-shot smoke、保留卷停栈、删卷重置。仅 gateway 绑定 localhost,详见 Docker guide。
- `TB_BASE_URL=https://tool-bridge.pdjjq.org TB_SK='…' pnpm smoke` — 线上冒烟(**smoke 不读 .env,两者都须显式传;缺任一项网络前 exit 1,不跳过认证检查**)。
- `npx tsx scripts/verify-revocation.ts` / `verify-device.ts` / `verify-plugin.ts` — 可重跑在线环境验收(需 TB_BASE_URL + TB_SK,会消耗目标环境真实资源)。
- `TB_BASE_URL=… TB_SK=… TB_MCP_NARROW_SK=… pnpm verify:mcp` — 在线 MCP consumer 出口验收;窄 SK 默认须能读 `system/status:get`,或用 `TB_MCP_NARROW_PATH/COMMAND/ARGS` 选另一无副作用工具。窄 list 为空会 fail closed,不得用零权限 SK 冒充 scope 收窄。
- `TB_BASE_URL=… TB_SK=… TB_SEARCH_NARROW_SK=… TB_SEARCH_ALLOWED_PREFIX=… pnpm verify:search` — 只读在线 Search验收;目标环境须预置 `日程`/`create document` 在允许前缀内外的 control fixtures,脚本沿 cursor拉全页后判 prefix/subset/strict-shrink。
- `pnpm --filter @tool-bridge/plugin-sdk build && pnpm --filter @tool-bridge/plugin-sdk pack --dry-run --json` — plugin-sdk 发布形状门;该包依赖 pnpm 应用 `publishConfig`,`npm pack --dry-run` 虽可 exit 0 但仍指向 source,不得作为可发布证据。
- `TB_TEST_MCP_URL=http://127.0.0.1:39002/mcp TB_ALLOW_INSECURE_HTTP=true pnpm --filter @tool-bridge/gateway test -- tool.integration.test.ts` — opt-in MCP E2E(先 `ECHO_MCP_PORT=39002 pnpm --filter @tool-bridge/gateway echo-mcp` 起兜底上游)。
- `TB_TEST_LIVE_HTTP=1 pnpm --filter @tool-bridge/gateway test -- tool.integration.test.ts` — opt-in 真实 HTTP 上游(postman-echo)。
- `TB_TEST_S3_* + TB_ALLOW_INSECURE_HTTP=true … context.integration.test.ts` — opt-in s3(本地 s3rver:`pnpm --filter @tool-bridge/gateway s3-mock`)。
- `TB_TEST_SDK_REMOTE=1 pnpm --filter @tool-bridge/sdk test` — opt-in SDK 反向连接生产全链路。

## .env 凭据状态(只记变量名与状态,绝不写值)

| 变量 | 状态 | 备注 |
|---|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | 已配置 | DJJ 账户;验证用 `wrangler whoami`,勿用 `/user/tokens/verify` |
| `CLOUDFLARE_API_TOKEN` | 空缺(注释掉) | 预期内:本地开发靠 wrangler OAuth,CI 时才需要 |
| `TB_DOMAIN` / `TB_BASE_URL` / `TB_NAME_PREFIX` | 已配置 | zone pdjjq.org;当前共享开发 BaseURL;资源命名前缀(=tb) |
| `TB_SECRET_ENCRYPTION_KEY` | 已配置(32B base64url) | SecretStore env-only 信任根;已同步 `wrangler secret put` |
| `TB_SK` | **已配置(= Admin SK)** | CLI/smoke/verify-* 脚本的默认凭证 |
| `TB_TEST_MCP_URL` / `TB_TEST_MCP_BEARER` | 空缺(注释掉) | opt-in MCP 测试用;见下方兜底 |
| `TB_TEST_S3_ENDPOINT` / `_ACCESS_KEY_ID` / `_SECRET_ACCESS_KEY` / `_BUCKET` | 空缺(注释掉) | opt-in s3 测试用;见下方兜底 |
| `TB_R2_ACCESS_KEY_ID` / `TB_R2_SECRET_ACCESS_KEY` | 空缺(注释掉) | `$ref` 预签名用;创建后 presign 主路径可 opt-in 复验 |

## 已知兜底路径(缺外部资源时)

- **真实上游 MCP 空缺** → `pnpm --filter @tool-bridge/gateway echo-mcp` 自建 echo MCP 兜底。
- **外部 S3 空缺** → 本地 s3rver mock opt-in 可重跑;或用 DJJ 账户 R2 的 S3 兼容 API 当"外部 S3"。
- **R2 预签名 AK 空缺** → 大对象走 `/~ref` 网关中转下载路由,功能不缺(生产实测通过)。

## 本机工具链(2026-07-06 核实)

| 工具 | 版本/状态 |
|---|---|
| node | v26.4.0 |
| pnpm | 11.10.0 |
| wrangler | 4.107.0,已 OAuth 登录(可访问 DJJ 与 Lightspeed 两账户) |
| gh | 2.96.0,已登录 Disdjj(有 repo scope,可访问 v1 私有仓库) |
| docker | CLI 29.2.1,守护进程可用(2026-07-08 核实;build/run/restart 镜像验收已过) |

**注意**:wrangler OAuth 下有多账户,所有 wrangler 命令须显式指定账户(`wrangler.jsonc` 已写 `account_id`;脚本内用 `CLOUDFLARE_ACCOUNT_ID`),否则报多账户歧义错误。

## 未竟事项(路线图,非进度账本)

- **工具搜索后续:**实现 semantic并同步外部 HTBP Draft。真实 D1 provision/deploy、admin/narrow `verify:search` 与线上 Dashboard exact-query 已在共享开发环境完成;正式 release 仍须在 PR 合入后从 clean main 重新部署。
- **安全后续:**Secret Reference 使用授权与 SDK/Node canonical-origin 已落地;DO/Node invoke 已重验 scope/registerPaths 与连接代际,Node 有确定性 barrier,共享开发 DO 也完成 155s hibernation与同 ID replacement。仍需设计 HttpOnly session+CSRF/BFF、分布式 rate limit,并保留 KV 最终一致边界。
- **发布积压(2026-08-18 本地版本):七包全部待发**,合入 main 后走 `gh workflow run release.yml -f dry-run=false` 一次发完(**不要在分支打 tag**)。local:app 0.5.0、cli 0.16.0、dashboard 0.14.0、gateway 0.9.0、plugin-sdk 0.5.0、sdk 0.9.0、server 0.4.0;registry latest 仍以发布当刻的 `pnpm release:plan` 为准。**顺序硬约束仍在:dashboard 必须早于 server**——`@tool-bridge/dashboard` 是 server 的 regular dependency(`workspace:*` 在 `pnpm pack` 时解析成具体版本);`release-plan.mjs` 已声明偏序并会把不可安装组合报成阻塞项。
- **内置集成的后续(P3,均非阻塞)**:①**legacy 记录清理** —— 既有部署里被(已删除的)`autoRegisterBinding` 写进 KV 的 `plugin:`/`pluginmeta:` 内置记录仍在库。**契约正确性已由 `resolveIntegration` 兜住**:`binding:` endpoint 的注册记录**不覆盖** catalog(否则那份"只在注册那刻抓过一次"的 `pluginmeta:` 快照会让 A3 在升级过的部署里原样复活——改了插件声明后平台仍按老快照校验挂载;判据是 endpoint 决定契约真源,`binding:` 的真源是构建产物,https 才是 KV 快照,已有三条测试钉住)。剩下的是**卫生问题**:那些残留记录会让 `plugin list` 列出内置项、`plugin catalog` 把它们标成 registered,需要一次清理路径;②**反索引 / B5** —— secret 删除时看不见"哪些节点在引用它"(当前只能全树扫 `registry.subtree('')` 反查,到 1000 挂载会成问题,同一处瓶颈也影响 `system/plugin catalog`);③**包级 `mountConfigSchema` 消费面** —— `PluginPackage.mountConfigSchema`(`core/src/plugin/package.ts`,插件包**安装分发单位**的 JSON Schema 配置)仍零消费点。**注意与 export 级 `mountConfigFields` 分层**:2026-08-15 已用 export 级 `mountConfigFields`(扁平字段)落地了"非凭证配置有声明面、CLI/Dashboard 据此校验与渲染带标签输入、从 `credentialFields` 里分出去"——那部分**不再是遗留**;这里剩下的只是 `PluginPackage` 那层(插件市场安装时收集配置)的 JSON Schema 版,与 export 挂载配置是两个不同层。
- **tool-bridge-template 外部模板仓库发布**:仓内 `template/` 已补齐 Deploy Button 两项 secret 预收集、binding 说明、当前已发布 gateway/dashboard 依赖并过隔离 install/tsc/wrangler dry-run；对外模板仍需同步发布。自定义域须配置 `TB_CANONICAL_ORIGIN`,Preview URLs 保持禁用。
- **plugin-feishu 遗留**:list-docs 需 drive:drive 应用权限(用户按需开通)。文档落点已定型团队 wiki(2026-07-08 实测):应用加入知识库成员并授**可编辑**(仅可阅读时 CreateWikiNode 报 131006)后,`create-doc` 传 `wiki_node` URL 建档/append/读回全通;团队 wiki 节点 https://awaken-intelligence.feishu.cn/wiki/AhxNw8VDAi0obtkqioyceAYgnZJ(Tool-Bridge)。可选优化:plugin 加 `FEISHU_DEFAULT_WIKI_NODE` 默认落点,待用户拍板。
- **端到端验收系统化**:Phase 5 A/B/E/C/D 已由本地与共享开发环境证据全部勾选;Compose/browser 最小矩阵尚未固化为长期 Playwright spec。全局 Done 仍需外部 HTBP Draft 与本分支合入 main。
- **中立层后续(2026-08-11 架构评估的 #1–#6):全部完成**——①独立包 `@tool-bridge/app`、②验证面迁移 + `tbApp.ts` 拆分、③plugin 托管形态去 CF 默认、④两个 SearchIndex adapter 收敛到 core `sqlSearchIndex.ts`、⑤部署叙事去 CF 默认(wrangler 去账户化 + provision 唯一 materializer)、⑥卫生项(zod 统一 v4、`no-explicit-any` 提 error)。**剩下的是已泄漏进协议的 CF 语义**,需显式记为约束而非默认:KV 最终一致进了安全模型、D1 Free 50-query 上限塑造了 `TOOL_SEARCH_*` 常量、DO hibernation 把心跳 keepalive 推进了协议。这三条不打算"去掉",而是要在协议文档里写成"任一宿主都必须满足的下界"。
- **遗留小项**:CLI deviceRuntime 迁移为 SDK 消费者;R2 Access Key 创建后 presign 主路径 opt-in 复验;共享开发环境 `device/demo-mac` 疑似测试残骸待确认清理;dashboard 本地开发流程(vite dev + wrangler dev 联调)下次涉及时补 guide;Cloudflare Analytics 自动 beacon 与严格 CSP 的策略需在平台侧决定。

## 遗留注意

- opt-in MCP E2E 退出码为 0,但 workerd 会打印 SDK sourcemap 诊断与一次 `Network connection lost` 文本,属 harness 噪声,不作为失败依据。
- 本机主 checkout(`~/.superset/projects/tool-bridge`)工作区有一份未提交 WIP(deviceSession→deviceHello 抽取重构);**部署必须从与 origin/main 零差异的干净工作区执行**(2026-07-07 部署即为避开该 WIP 从干净 worktree 出发)。
