# First initialize one replica and configure Redis through Dashboard.
# The claim must support ReadWriteMany and contain the same initialized bootstrap state.
replicaCount: 2
bootstrap:
  existingClaim: tool-bridge-bootstrap-rwx
  shared: true
