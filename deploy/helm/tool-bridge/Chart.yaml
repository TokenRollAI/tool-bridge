apiVersion: v2
name: tool-bridge
description: >-
  Node self-hosted Tool Bridge with paired setup, PostgreSQL and S3.
type: application
# chart 版本与 appVersion 解耦:appVersion 跟随 @tool-bridge/server 的镜像 tag。
version: 0.2.0
appVersion: "0.22.0"
keywords:
  - tool-bridge
  - htbp
  - mcp
  - agent-tools
home: https://github.com/TokenRollAI/tool-bridge
sources:
  - https://github.com/TokenRollAI/tool-bridge
