apiVersion: v1
kind: Service
metadata:
  name: {{ include "tool-bridge.fullname" . }}
  labels:
    {{- include "tool-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "tool-bridge.selectorLabels" . | nindent 4 }}
