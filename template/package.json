{
  "name": "tool-bridge-template",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Deploy your own tool-bridge gateway to Cloudflare Workers in one click",
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/copy-ui.mjs && wrangler dev",
    "deploy": "node scripts/copy-ui.mjs && wrangler deploy",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@tool-bridge/dashboard": "^0.24.0",
    "@tool-bridge/gateway": "^0.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.123.0"
  },
  "cloudflare": {
    "bindings": {
      "TB_STATE": {
        "description": "Authoritative state (D1): node tree config, SecretKey hashes, encrypted secrets, plugin manifests."
      },
      "TB_SEARCH": {
        "description": "Tool-search index (D1, same database): weighted LIKE backing for `~search`."
      },
      "TB_R2": {
        "description": "Deployment default Store, Context objects, and large `$ref` payloads."
      },
      "TB_DEVICE": {
        "description": "One Durable Object per connected device (WebSocket hibernation)."
      },
      "TB_BOOTSTRAP_ADMIN_SK": {
        "description": "Required. Generate once with `node -e \"console.log('tbk_'+require('crypto').randomBytes(32).toString('base64url'))\"`, save it in a password manager, and paste it here."
      },
      "TB_SECRET_ENCRYPTION_KEY": {
        "description": "Required. SecretStore root key; generate with `node -e \"console.log(require('crypto').randomBytes(32).toString('base64url'))\"`."
      }
    }
  }
}
